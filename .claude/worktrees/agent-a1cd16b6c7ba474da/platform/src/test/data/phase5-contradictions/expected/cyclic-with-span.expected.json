{
  "scenario": "cyclic-with-span",
  "description": "A→B and B→A active causal edges with temporal_span=P1D on both. Heuristic must NOT flag — temporal separation makes the cycle valid.",
  "data_set_level": 1,
  "fixture": "phase5-contradictions/fixtures/cyclic-with-span.sql",
  "phases_exercised": ["detection"],

  "stages": [
    {
      "stage": "detection",
      "driver": "service_call",
      "service_call": "detectCyclicCausal",
      "assertions": [
        {
          "type": "row_count",
          "table": "contradictions",
          "filter": { "contradiction_type": "cyclic_causal" },
          "expected": 0,
          "because": "Cycles with temporal_span on both edges are legitimate (periodic feedback) and must not be flagged"
        }
      ]
    }
  ],

  "graduation_notes": "Pair with cyclic-no-span (positive). Add cyclic-mixed-span (only one edge has span — heuristic flags because the requirement is BOTH NULL)."
}
