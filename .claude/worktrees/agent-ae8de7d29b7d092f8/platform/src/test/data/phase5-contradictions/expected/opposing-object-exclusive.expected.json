{
  "scenario": "opposing-object-exclusive",
  "description": "Two active works_at facts with different objects (supersession bypassed). Heuristic MUST NOT flag because works_at is_exclusive=true; supersession owns this case.",
  "data_set_level": 1,
  "fixture": "phase5-contradictions/fixtures/opposing-object-exclusive.sql",
  "phases_exercised": ["detection"],

  "stages": [
    {
      "stage": "detection",
      "driver": "service_call",
      "service_call": "detectOpposingObjects",
      "assertions": [
        {
          "type": "row_count",
          "table": "contradictions",
          "filter": { "contradiction_type": "opposing_object" },
          "expected": 0,
          "because": "Exclusive predicates must be excluded by the heuristic — supersession is the canonical resolution path"
        }
      ]
    }
  ],

  "benchmark_targets": {
    "detection_latency_ms": { "max": 200, "because": "Two facts, trivial scan" },
    "false_positive_rate_on_exclusive_predicates": { "max": 0.0, "because": "Zero tolerance for false flags on supersession-handled predicates" }
  },

  "graduation_notes": "Negative test for the exclusive-predicate exclusion. Pair with opposing-object-simple (positive test on `knows`)."
}
