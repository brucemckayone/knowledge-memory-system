# Large derived artifacts (regenerated by prep_flat_baseline.py + the TS harness).
# The dataset itself lives in the pinned upstream/ submodule; these are byproducts.
flat-baseline-entity-questions.json
flat-baseline-embed-cache.json
flat-baseline-frozen.json
temporal-experiment-simple-entity.json
