# Large derived artifacts (regenerated by prep_i1_local.py + the TS harness).
# The dataset itself lives under data/ (ignored by benchmarks/.gitignore); these
# are byproducts. prep_i1_local.py IS tracked.
i1-local-cut.json
i2-multi-cut.json
i1-embed-cache.json
i1-vecs.bin
i1-keys.jsonl
i1-frozen.json
