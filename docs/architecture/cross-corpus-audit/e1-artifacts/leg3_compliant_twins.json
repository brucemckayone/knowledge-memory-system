[
  {
    "pair_id": "R3-1",
    "built_for": "R.3",
    "code": "class CovarianceScratch {\npublic:\n    explicit CovarianceScratch(int n) : size_(n), buffer_(n * n) {}\n    double* data() { return buffer_.data(); }\nprivate:\n    int size_;\n    std::vector<double> buffer_;   // owns the memory via RAII\n};"
  },
  {
    "pair_id": "R3-2",
    "built_for": "R.3",
    "code": "std::unique_ptr<Ephemeris> loadEphemeris(const std::string& rinexPath) {\n    auto eph = std::make_unique<Ephemeris>();\n    if (!parseRinexNav(rinexPath, *eph)) {\n        return nullptr;\n    }\n    return eph;\n}"
  },
  {
    "pair_id": "R3-3",
    "built_for": "R.3",
    "code": "class MeasurementBuffer {\npublic:\n    explicit MeasurementBuffer(std::size_t capacity) : capacity_(capacity) {\n        observations_.reserve(capacity);\n    }\n    void push(const RawObservation& obs) {\n        if (observations_.size() < capacity_) observations_.push_back(obs);\n    }\nprivate:\n    std::size_t capacity_;\n    std::vector<RawObservation> observations_;\n};"
  },
  {
    "pair_id": "R3-4",
    "built_for": "R.3",
    "code": "class CorrectionStream {\npublic:\n    // decoder is supplied and owned by the caller (e.g. held in a unique_ptr\n    // inside the session object); CorrectionStream only observes it.\n    explicit CorrectionStream(RtcmDecoder* decoder) : decoder_(decoder) {}\n    void onBytes(const uint8_t* data, std::size_t n) { decoder_->feed(data, n); }\nprivate:\n    RtcmDecoder* decoder_;   // non-owning\n};"
  },
  {
    "pair_id": "C131-2",
    "built_for": "C.131",
    "code": "// No invariant to protect, so there is nothing for a getter/setter to add:\n// expose the value directly.\nstruct SatelliteId {\n    int prn;\n};"
  },
  {
    "pair_id": "C131-3",
    "built_for": "C.131",
    "code": "class KalmanFilterState {\npublic:\n    const Eigen::MatrixXd& getCovariance() const { return covariance_; }\n    void setCovariance(const Eigen::MatrixXd& p) {\n        // Numerical noise can break symmetry; enforce the invariant here.\n        covariance_ = 0.5 * (p + p.transpose());\n    }\nprivate:\n    Eigen::MatrixXd covariance_;\n};"
  },
  {
    "pair_id": "C131-4",
    "built_for": "C.131",
    "code": "class ReceiverClock {\npublic:\n    // Internally the clock model tracks bias in integer nanoseconds to avoid\n    // float drift; the public API performs the unit conversion for callers.\n    double getBiasSeconds() const { return static_cast<double>(biasNanos_) * 1e-9; }\n    void setBiasSeconds(double bias) { biasNanos_ = static_cast<int64_t>(bias * 1e9); }\nprivate:\n    int64_t biasNanos_ = 0;\n};"
  },
  {
    "pair_id": "F2-1",
    "built_for": "F.2",
    "code": "Fix parseNmeaGga(const std::string& line) {\n    if (!nmeaChecksumValid(line)) throw std::invalid_argument(\"bad checksum\");\n    return fixFromGgaFields(splitNmeaFields(line));\n}\n\nvoid storeFix(FixDatabase& db, const Fix& fix) {\n    db.insert(fix);\n}\n// The caller composes parseNmeaGga() + storeFix(); logging happens at the\n// call site, not inside either single-purpose function."
  },
  {
    "pair_id": "F2-2",
    "built_for": "F.2",
    "code": "NavMessage readEphemerisRecord(const std::string& path) {\n    std::ifstream in(path);\n    std::string record((std::istreambuf_iterator<char>(in)), {});\n    return parseRinexNavRecord(record);\n}\n\nbool isValidToeWeek(const NavMessage& nav) {\n    return nav.toeWeek >= 0 && nav.toeWeek <= 4095;\n}\n// The caller reads, validates, then updates AlmanacCache and logs, instead\n// of one function doing all four steps."
  },
  {
    "pair_id": "ES1-1",
    "built_for": "ES.1",
    "code": "const SatelliteState* findByPrn(const std::vector<SatelliteState>& sats, int prn) {\n    auto it = std::find_if(sats.begin(), sats.end(),\n                            [prn](const SatelliteState& s) { return s.prn == prn; });\n    return it != sats.end() ? &*it : nullptr;\n}"
  },
  {
    "pair_id": "ES1-2",
    "built_for": "ES.1",
    "code": "double sumSquaredResiduals(const std::vector<double>& residuals) {\n    return std::accumulate(residuals.begin(), residuals.end(), 0.0,\n                            [](double acc, double r) { return acc + r * r; });\n}"
  },
  {
    "pair_id": "ES1-3",
    "built_for": "ES.1",
    "code": "const Observation* worstResidual(const std::vector<Observation>& obs) {\n    auto it = std::max_element(obs.begin(), obs.end(),\n        [](const Observation& a, const Observation& b) {\n            return std::abs(a.residual) < std::abs(b.residual);\n        });\n    return it != obs.end() ? &*it : nullptr;\n}"
  },
  {
    "pair_id": "ES1-4",
    "built_for": "ES.1",
    "code": "bool anySatelliteBelowMask(const std::vector<SatelliteState>& sats, double maskDeg) {\n    return std::any_of(sats.begin(), sats.end(),\n                        [maskDeg](const SatelliteState& s) { return s.elevationDeg < maskDeg; });\n}"
  },
  {
    "pair_id": "C4-3",
    "built_for": "C.4",
    "code": "class ObservationRingBuffer {\npublic:\n    explicit ObservationRingBuffer(std::size_t capacity) : capacity_(capacity) {}\n    bool isFull() const { return size_ == capacity_; }\nprivate:\n    std::size_t capacity_;\n    std::size_t size_ = 0;\n};"
  },
  {
    "pair_id": "C4-4",
    "built_for": "C.4",
    "code": "class SatelliteTracker {\npublic:\n    bool isAboveMask(double elevationDeg) const { return elevationDeg >= elevationMaskDeg_; }\nprivate:\n    double elevationMaskDeg_ = 10.0;\n    std::vector<int> trackedPrns_;\n};"
  },
  {
    "pair_id": "C35-4",
    "built_for": "C.35",
    "code": "class EphemerisSource {\npublic:\n    virtual NavMessage load(int prn) = 0;\n    virtual ~EphemerisSource() = default;\n};\n\nclass GpsEphemerisSource : public EphemerisSource {\npublic:\n    GpsEphemerisSource() : cache_(new NavMessage[32]) {}\n    NavMessage load(int prn) override { return cache_[prn % 32]; }\n    ~GpsEphemerisSource() override { delete[] cache_; }\nprivate:\n    NavMessage* cache_;\n};"
  },
  {
    "pair_id": "F2-5",
    "built_for": "F.2",
    "code": "struct FixRecord { double latitude, longitude, altitude; int fixQuality; };\nclass GgaIngestor {\npublic:\n    static FixRecord parseLine(const std::string& line) {\n        std::stringstream ss(line);\n        std::vector<std::string> fields;\n        std::string token;\n        while (std::getline(ss, token, ',')) fields.push_back(token);\n        return FixRecord{std::stod(fields[2]) / 100.0, std::stod(fields[4]) / 100.0,\n                          std::stod(fields[9]), std::stoi(fields[6])};\n    }\n\n    void recordStatistics(const FixRecord& rec) {\n        totalFixesSeen_++;\n        if (rec.fixQuality == 0) invalidFixCount_++;\n        averageAltitude_ = (averageAltitude_ * (totalFixesSeen_ - 1) + rec.altitude) / totalFixesSeen_;\n    }\n\n    void logFix(const FixRecord& rec) {\n        logFile_ << rec.latitude << \",\" << rec.longitude << \",\" << rec.altitude << \"\\n\";\n    }\n\nprivate:\n    int totalFixesSeen_ = 0;\n    int invalidFixCount_ = 0;\n    double averageAltitude_ = 0.0;\n    std::ofstream logFile_{\"gga_track.log\", std::ios::app};\n};"
  },
  {
    "pair_id": "F2-6",
    "built_for": "F.2",
    "code": "struct RtcmMessage { int messageType; std::vector<uint8_t> payload; };\nint connectToCaster(const std::string& host, int port);\nclass RtcmStreamReader {\npublic:\n    std::optional<RtcmMessage> readNextMessage() {\n        uint8_t header[3];\n        if (recv(socketFd_, header, sizeof(header), MSG_WAITALL) <= 0) return std::nullopt;\n        if (header[0] != 0xD3) return std::nullopt;\n        uint16_t length = ((header[1] & 0x03) << 8) | header[2];\n        std::vector<uint8_t> payload(length);\n        recv(socketFd_, payload.data(), length, MSG_WAITALL);\n        return RtcmMessage{(payload[0] << 4) | (payload[1] >> 4), std::move(payload)};\n    }\n\n    void reconnect() {\n        close(socketFd_);\n        socketFd_ = connectToCaster(host_, port_);\n        reconnectAttempts_++;\n    }\n\nprivate:\n    int socketFd_;\n    std::string host_;\n    int port_;\n    int reconnectAttempts_ = 0;\n};"
  },
  {
    "pair_id": "F2-7",
    "built_for": "F.2",
    "code": "class PositionFilter {\npublic:\n    void updateWithRange(double measuredRange, double predictedRange, double measurementVariance) {\n        double innovation = measuredRange - predictedRange;\n        double gain = covariance_ / (covariance_ + measurementVariance);\n        state_ += gain * innovation;\n        covariance_ = (1.0 - gain) * covariance_;\n    }\n\n    void logDiagnostics() const {\n        std::cout << \"state=\" << state_ << \" covariance=\" << covariance_ << \"\\n\";\n    }\n\n    void writeCheckpoint(const std::string& path) const {\n        std::ofstream ckpt(path, std::ios::binary | std::ios::trunc);\n        ckpt.write(reinterpret_cast<const char*>(&state_), sizeof(state_));\n        ckpt.write(reinterpret_cast<const char*>(&covariance_), sizeof(covariance_));\n    }\n\nprivate:\n    double state_ = 0.0;\n    double covariance_ = 1.0;\n};"
  },
  {
    "pair_id": "C4-5",
    "built_for": "C.4",
    "code": "class GeodeticConverter {\npublic:\n    GeodeticConverter(double semiMajorAxis, double flattening)\n        : semiMajorAxis_(semiMajorAxis), flattening_(flattening) {}\n    struct Geodetic { double lat, lon, alt; };\n\n    Geodetic ecefToGeodetic(double x, double y, double z) const {\n        double e2 = flattening_ * (2 - flattening_);\n        double lon = std::atan2(y, x);\n        double p = std::sqrt(x * x + y * y);\n        double lat = std::atan2(z, p * (1 - e2));\n        for (int i = 0; i < 5; ++i) {\n            double sinLat = std::sin(lat);\n            double n = semiMajorAxis_ / std::sqrt(1 - e2 * sinLat * sinLat);\n            lat = std::atan2(z + e2 * n * sinLat, p);\n        }\n        double sinLat = std::sin(lat);\n        double n = semiMajorAxis_ / std::sqrt(1 - e2 * sinLat * sinLat);\n        double alt = p / std::cos(lat) - n;\n        return {lat * 180.0 / M_PI, lon * 180.0 / M_PI, alt};\n    }\n\nprivate:\n    double semiMajorAxis_;\n    double flattening_;\n};"
  },
  {
    "pair_id": "C4-6",
    "built_for": "C.4",
    "code": "class NmeaEncoder {\npublic:\n    explicit NmeaEncoder(std::string talkerId) : talkerId_(std::move(talkerId)) {}\n\n    uint8_t computeChecksum(const std::string& body) const {\n        uint8_t checksum = 0;\n        for (char c : talkerId_) {\n            checksum ^= static_cast<uint8_t>(c);\n        }\n        for (char c : body) {\n            checksum ^= static_cast<uint8_t>(c);\n        }\n        return checksum;\n    }\n\n    std::string buildSentence(const std::string& body) const {\n        uint8_t cs = computeChecksum(body);\n        std::ostringstream oss;\n        oss << \"$\" << talkerId_ << body << \"*\" << std::hex << std::setw(2) << std::setfill('0') << int(cs);\n        return oss.str();\n    }\n\nprivate:\n    std::string talkerId_;\n};"
  },
  {
    "pair_id": "C4-7",
    "built_for": "C.4",
    "code": "class EphemerisValidator {\npublic:\n    explicit EphemerisValidator(int referenceGpsWeek) : referenceGpsWeek_(referenceGpsWeek) {}\n\n    bool isValidWeekNumber(int week) const {\n        return week >= referenceGpsWeek_ - 1024 && week <= referenceGpsWeek_ + 1;\n    }\n\n    bool isCurrent(int week, int minValidWeek) const {\n        return isValidWeekNumber(week) && week >= minValidWeek && week <= referenceGpsWeek_;\n    }\n\nprivate:\n    int referenceGpsWeek_;\n};"
  },
  {
    "pair_id": "C35-5",
    "built_for": "C.35",
    "code": "class CorrectionSource {\npublic:\n    virtual std::vector<uint8_t> fetchNextMessage() = 0;\n    virtual bool isConnected() const = 0;\n    virtual ~CorrectionSource() = default;\n};\nclass NtripCorrectionSource : public CorrectionSource {\npublic:\n    std::vector<uint8_t> fetchNextMessage() override {\n        return readFromSocket();\n    }\n    bool isConnected() const override { return connected_; }\n\nprivate:\n    std::vector<uint8_t> readFromSocket() { return {}; }\n    bool connected_ = false;\n};\nvoid releaseSource(CorrectionSource* source) {\n    delete source;\n}"
  },
  {
    "pair_id": "C35-6",
    "built_for": "C.35",
    "code": "class PositioningFilter {\npublic:\n    virtual void predict(double dt) = 0;\n    virtual void update(const std::vector<double>& measurement) = 0;\n    virtual Eigen::Vector3d state() const = 0;\n    virtual ~PositioningFilter() = default;\n};\nclass ExtendedKalmanFilter : public PositioningFilter {\npublic:\n    void predict(double dt) override { state_ += velocity_ * dt; }\n    void update(const std::vector<double>& measurement) override {\n        state_.x() = 0.9 * state_.x() + 0.1 * measurement[0];\n    }\n    Eigen::Vector3d state() const override { return state_; }\n\nprivate:\n    Eigen::Vector3d state_{0, 0, 0};\n    Eigen::Vector3d velocity_{0, 0, 0};\n};\nstd::vector<std::unique_ptr<PositioningFilter>> buildFilterBank() {\n    std::vector<std::unique_ptr<PositioningFilter>> bank;\n    bank.push_back(std::make_unique<ExtendedKalmanFilter>());\n    return bank;\n}"
  },
  {
    "pair_id": "C35-7",
    "built_for": "C.35",
    "code": "struct EpochTick { double timestamp; };\nclass TimeEpochListener {\npublic:\n    virtual void onEpoch(const EpochTick& tick) = 0;\n    virtual void onGapDetected(double gapSeconds) = 0;\n\nprotected:\n    ~TimeEpochListener() = default;\n};\nclass LoggingEpochListener : public TimeEpochListener {\npublic:\n    void onEpoch(const EpochTick& tick) override { logStream_ << tick.timestamp << \"\\n\"; }\n    void onGapDetected(double gapSeconds) override { logStream_ << \"gap: \" << gapSeconds << \"\\n\"; }\n\nprivate:\n    std::ofstream logStream_{\"epoch.log\", std::ios::app};\n};\nclass EpochDispatcher {\npublic:\n    void registerListener(TimeEpochListener* listener) { listeners_.push_back(listener); }\n\nprivate:\n    std::vector<TimeEpochListener*> listeners_;\n};"
  },
  {
    "pair_id": "C131-5",
    "built_for": "C.131",
    "code": "class SatelliteMeasurement {\npublic:\n    explicit SatelliteMeasurement(double pseudorange) : pseudorange_(pseudorange) {\n        isPlausible_ = pseudorange_ > 1.0e7 && pseudorange_ < 1.0e8;\n    }\n\n    double pseudorange() const { return pseudorange_; }\n    void setPseudorange(double value) {\n        pseudorange_ = value;\n        isPlausible_ = value > 1.0e7 && value < 1.0e8;\n    }\n\n    bool isPlausible() const { return isPlausible_; }\n\nprivate:\n    double pseudorange_;\n    bool isPlausible_;\n};\nvoid applyClockCorrection(SatelliteMeasurement& measurement, double clockBiasMeters) {\n    measurement.setPseudorange(measurement.pseudorange() - clockBiasMeters);\n}"
  },
  {
    "pair_id": "C131-6",
    "built_for": "C.131",
    "code": "class AntennaCalibration {\npublic:\n    explicit AntennaCalibration(const Eigen::Vector3d& offset) : offset_(offset) {}\n\n    Eigen::Vector3d applyTo(const Eigen::Vector3d& rawPosition, const Eigen::Matrix3d& attitude) const {\n        return rawPosition - attitude * offset_;\n    }\n\nprivate:\n    Eigen::Vector3d offset_;\n};\nEigen::Vector3d correctedPosition(const AntennaCalibration& cal, const Eigen::Vector3d& rawPosition, const Eigen::Matrix3d& attitude) {\n    return cal.applyTo(rawPosition, attitude);\n}"
  }
]