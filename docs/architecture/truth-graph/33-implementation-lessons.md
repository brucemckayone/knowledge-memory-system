# Implementation lessons learned

Per-bead and cross-bead wisdom accumulated during the implementation phase of the **May 2026 review/hardening cycle** (epic `nmemo-2yv`).

The review cycle (`docs/architecture/truth-graph/31-review-cycle-synthesis.md`) produced 120 findings across 13 feature reviews + 1 cross-feature synthesis. Each finding's `## Decision (locked YYYY-MM-DD)` section is the locked spec; the `epic-cycle-implementation` skill drives those decisions into the codebase one bead at a time. This file accumulates the lessons that emerge during implementation — patterns that hold across beads, surprises worth flagging, conventions that crystallise as the cycle progresses.

**Cross-references:**
- Doc 31 §2 — theme inventory (T1/T7/T8/T9/T10/T11/T12/T13 + transport divergence).
- Doc 31 §5 — prioritised landing roadmap.
- `~/.claude/skills/epic-cycle-implementation/SKILL.md` — the orchestrator skill that appends to this file.

**Entry format:**

```markdown
## YYYY-MM-DD

### nmemo-2yv.<N> — <one-line title>

- **Lesson:** <the durable insight, written so a future reader can apply it without context>.
- **Surprise:** (optional) <what tripped us up — usually a doc-vs-code drift, a tool quirk, a Windows-isms issue>.
- **Pattern noted:** (optional) <a pattern this bead is an instance of, with pointers to related beads / themes>.
```

One **Lesson** per entry is mandatory. **Surprise** and **Pattern noted** are surfaced only when they carry forward.

Cross-project gotchas (anything that isn't Mnemo-specific) also go to `bd remember "<one-liner>"` for `bd memories <keyword>` retrieval.

---

<!-- Entries below this line, newest at the bottom. -->

## 2026-05-25

### nmemo-2yv.63 — merge_entities() FK violation on contradictions (T13 + post-merge integrity)

- **Lesson:** When a later migration adds a FK from a new table back to a long-established parent (here: `contradictions.entity_id → entities(id)` in mig 011) WITHOUT an ON DELETE clause, the parent's merge/delete logic must be updated in the SAME landing. PostgreSQL defaults to NO ACTION; the merge SQL in mig 005 (predating mig 011) had no reason to know about a future FK. Audit pattern: whenever adding a FK to entities, run `git grep "DELETE FROM public.entities"` to check the merge_entities() body needs a re-point step.
- **Surprise:** The bead's recommendation was "re-point rather than CASCADE" — preserving the contradiction trail across merges. But the partial UNIQUE INDEX `idx_contradictions_unique_active` (covering `entity_id` as one of its keys) means a naive `UPDATE contradictions SET entity_id = target_id` can violate the index if both source and target carry an unresolved contradiction with the same (type, fact_a, fact_b, edge_a, edge_b). The dedup DELETE has to mirror the index expression EXACTLY — same COALESCE sentinel UUID and same column tuple. Mis-aligning either would either over-delete or under-delete and fail the merge at the UPDATE.
- **Pattern noted:** PL/pgSQL multi-step migrations like merge_entities() are append-shaped — each "re-point step" gets bolted on. Without a discipline of "every new FK to entities triggers a paired merge-step", these accumulate as latent bugs. Worth filing a future check: a CI lint that verifies every FK with `REFERENCES public.entities` has a corresponding re-point step in merge_entities() or an explicit `ON DELETE CASCADE`/`SET NULL` clause.

### nmemo-2yv.38 — resolveContradiction transactional correctness (concurrency hardening)

- **Lesson:** Threading an optional `tx?: Tx` through mutation primitives (`expireFact`, `invalidateFact`, `expireCausalEdge`, `reviseCausalEdge`) is a clean refactor — the `runUpdate(tx)` helper used either with the passed-in tx or under a freshly-opened `db.transaction(runUpdate)` keeps the existing callers backwards-compatible while letting multi-step orchestrators (here: `resolveContradiction`) own the atomicity boundary. The exported `Tx = Parameters<Parameters<typeof db.transaction>[0]>[0]` type alias in `db/index.ts` is the cheapest way to surface drizzle-orm's transaction type without dragging in pg-core internals.
- **Surprise (caught by /code-review subagent):** Threading tx through the primitives is necessary but not sufficient for full atomicity. `expireFact` and `invalidateFact` delegate to `createCausalEvent` and `cascadeFactExpiry` AFTER their inner tx body returns — those delegations always run on the module-level `db` pool, even when the caller supplied a `tx`. If the outer tx rolls back (e.g. closing UPDATE contradictions throws), the causal_event and cascade-expired edges have already committed on separate connections and stay behind as orphans. This is the next domino — tracked as the bead's "Broader improvement note" follow-up, deferred from .38 by design.
- **Pattern noted:** When extending a primitive with an "optional outer transaction" parameter, the post-mutation side effects (audit emissions, cascade work) usually want the SAME tx-aware treatment. Not threading them is the smaller change but leaves a latent orphan-on-rollback. Whenever this pattern lands again (entity merges, etc.), thread tx ALL the way through or document the partial-atomicity explicitly. The /code-review subagent's "high-confidence" verdict on this gap is correct — the bead's locked scope just chose to defer it.

### nmemo-2yv.37 — edge-mutating contradiction resolution (T13 half-built pipeline)

- **Lesson:** When a polymorphic enum (here: `resolution_type`) dispatches through a TypeScript switch, the `default: const _exhaustive: never` pattern is what catches drift — but ONLY if every layer (DB CHECK, MCP enum, HTTP body cast, agent prompt) is also extended in lockstep. The TS exhaustiveness check protects the dispatcher, not the boundary layers above it. For new vocabulary, edit the union LAST so any layer you missed becomes a compile error against the (already-extended) switch's exhaustiveness verifier. Counter-pattern: don't edit the union first, because then the switch happily accepts unhandled values during the in-progress edits.
- **Surprise:** The bead's "Scoped fix" mentioned six layers (migration, contradictions.ts, causal-agent.ts, index.ts, reasoning_agent.py, doc 16). The seventh (HTTP body inline type union at index.ts:673-676) was visible only as a `body.resolution_type as` cast — not as a type imported from the service layer. This is a pre-existing weak-typing seam that lets the union drift silently; future hardening could replace the cast with `import type { ResolutionType }`.
- **Pattern noted:** The bead listed a "Future direction" footnote about replacing the static enum-dispatcher with a per-contradiction agent call. Option B (this bead) is a stopgap. The TOCTOU race surfaced by the code-review subagent (`expireCausalEdge` is a no-op on already-expired edges → if another path expires the edge between detection and resolution, the contradiction closes with no history row) is a known consequence of the static-dispatch design; the "Future direction" rewrite obviates it.

### nmemo-2yv.60 — resolve_candidate enum / merge_candidates CHECK alignment (T7 naming mismatch)

- **Lesson:** When a CHECK constraint enumerates vocabulary that other layers (tool schemas, agent prompts, table names) also enumerate, a divergence is silent and total — every write of the new value hits the CHECK and fails on the close path, while the upstream side-effect (here: the same_as_links row) lands via a different write path. The bug only surfaces if you check the candidate row's *post-resolution* status rather than the link table. This is a generalisable test-design lesson: when a pipeline writes to two tables and an error in one is recoverable into the other, end-to-end tests must assert *both* terminal states, not just the visible one.
- **Surprise:** The bead's Scoped fix had three steps but the third ("integration test against the DB write path") was the one carrying the regression-guard value. Steps 1 + 2 (migration + grep-for-readers) could have been done blindly with low-confidence safety; step 3 is what locks down the contract. The instinct to land migrations without an exercising test would have allowed a future revert of the CHECK to slip through unnoticed.
- **Pattern noted:** Doc 27 §2.2 already discusses 'same_as' as a training-label vocabulary. The schema CHECK in 003 predates that discussion. Whenever a vocabulary discussion lands in a design doc, run `grep "CHECK.*IN (" platform/src/db/migrations/*.sql` against the new vocabulary to detect drift early. Future work: vocabulary-alignment pass before reasoning_reports (per the bead's "Future direction" note).

### nmemo-2yv.112 — Pi bridge port default 3001 → 3099

- **Lesson:** When a bead's Scoped fix lists multiple edits and one of them is incompatible with the project's actual dev environment, prefer the smallest faithful change that satisfies the bead's *acceptance* over a literal application that introduces a regression. Here the bead asked for `"bridge": "PI_BRIDGE_PORT=3099 tsx src/services/pi-agent-bridge.ts"` in `package.json`, but inline `KEY=value cmd` syntax doesn't work in Windows cmd/PowerShell (the user's primary shell per CLAUDE.md), so `npm run bridge` would have regressed. Skipping that step was the right call because the code-default change at `pi-agent-bridge.ts:38` already satisfies the actual acceptance bullet ("npm run bridge starts the bridge on 3099 with no extra env setup needed"). Always check the bead's *acceptance contract* vs the *implementation list* — the former is the binding spec.
- **Surprise:** Bullet 5 said "deleting ml-services/.env and running make bridge + make ml works (llm.py picks up the 3099 code default)" — but deleting `.env` also strips `LLM_PROVIDER=pi`, which makes `LLM_PROVIDER` default to `claude`, which means `PiBridgeProvider` is never instantiated and the bridge-URL default never gets exercised. The bullet as literally written passes vacuously. To honour the bullet's *intent* (verify the code default), unset `PI_BRIDGE_URL` while keeping `LLM_PROVIDER=pi` and instantiate `PiBridgeProvider` directly.
- **Pattern noted:** Bullet 6 ("Port table in doc 29") referenced a doc owned by a separate bead (`.110`). This is a recurring shape in this review cycle — beads sometimes carry forward dependencies that can't be satisfied at their own implementation time. The skill should treat such bullets as "deferred to the owning bead" rather than halting acceptance.

### nmemo-2yv.123 — /api/mcp-health probe retargeted to graph-mcp.ts

- **Lesson:** When two callers share a piece of resolved config (here: a filesystem path), extracting a single resolver that both routes through is the cheap way to make drift a compile-time error. The probe and the per-actor MCP config writer now both call `getGraphMcpScriptPath()`; future changes to where `graph-mcp.ts` lives must update one place. The pattern is worth applying any time a "production config writer" and an "observability probe" of that same production thing diverge.
- **Surprise:** The bead's `## Result` section claimed `src/index.ts` was the only consumer of `checkCausalMcpHealth`, but `grep` turned up two more active call sites — `scripts/smoke-mcp-tools.ts` and `src/test/harness/causal-integration.test.ts`. The bead's `## Acceptance` was stricter ("old name no longer exists in the codebase"), so all three had to be renamed. **Implication:** always grep the symbol myself before relying on the bead's narrative inventory. The Acceptance section is the contract; the Result section is a snapshot that may have aged.
- **Pattern noted:** Renaming a function name that embeds a stale concept (here: `Causal` when the production server is the unified `graph` server) sweeps cleaner if done in the same diff as the underlying retarget — leaving the rename for "later" entrenches the wrong identity at every call site. This pairs with the bead's `## Decision` rejection of shape (a) "one-line retarget only" for exactly the same reason.

## 2026-05-26

### nmemo-2yv.83 — drift-reconciliation post-compute trigger (T13 / G4 half-built pipeline)

- **Lesson:** When a bead spec includes example SQL or example payloads, the field names may be from the *consumer's* (payload-side) layer, not the *storage* (column-name) layer. The bead's `## Decision` SELECT used `source_cluster_id` (the agent's `ReconciliationDriftRequest` payload field). The actual column in `entity_drift_events` is `cluster_id_at_detection` (per 016 §3.2). Copy-pasting the bead's SQL verbatim produced a Postgres `column "source_cluster_id" does not exist`. **General rule:** when a bead author writes example SQL, treat field names in the example as conceptual; before writing the query, read the target table's schema and translate. Cheap fix, easy to miss — the test caught it immediately, but a fix landed without a test wouldn't have.
- **Surprise:** The bead's locked spec read "On agent success, the entity_drift_events row has reconciliation_run_id set referencing the agent's generated row in reasoning_reports / gardening_reports / appropriate audit table." Reading the ml-services `/reconciliation-agent/drift` endpoint (`drift.py:771-803`) showed the agent returns ONLY a `{result: str}` — it never writes to reasoning_reports or any audit table. The bead's success-path narrative was aspirational about a future where the agent self-writes audit rows. The faithful minimum-change interpretation: the platform helper writes the audit row itself, capturing the agent's text in `reasoning_reports` with `mode='patrol'` (closest legal CHECK value) and `actions_taken.source='reconciliation_drift_agent'` for disambiguation. A future bead can either add a `'drift_reconciliation'` mode (CHECK relaxation, ~5-line migration) or push the audit-write into the agent itself.
- **Pattern noted:** In fire-and-forget helpers invoked via `void helper()`, any *setup* work (dynamic imports, config resolution) MUST live inside the outer try/catch. A failure above the try escapes as an unhandled promise rejection (Node 20+ default = process death on strict mode). The sibling `triggerCrossClusterAfterCompute` (index.ts:999) already follows this — its dynamic import is inside the try at line 1001. My first cut put the dynamic import on the line above the try; code-review caught it. **Rule:** for any `export async function trigger*(...)` matching the fire-and-forget pattern, the FIRST line of the function body should be `try {` and ALL awaits live inside.
- **Pattern noted (carry-forward, deferred):** Cross-helper concurrency. `triggerCrossClusterAfterCompute` uses `pg_try_advisory_xact_lock(hashtext(LOCK_KEY))` to deduplicate overlapping invocations. The new drift-reconciliation helper does NOT (bead spec didn't require it). Two overlapping `/api/drift/compute` calls would double-invoke the LLM and orphan one of two `reasoning_reports` rows per drift event. Bead `.88` is the natural home for sweeping both helpers into a services module — at that point, also adopt the advisory lock pattern uniformly. Tracked in the bead's deferred-follow-ups note for whoever picks up `.88`.

### nmemo-2yv.87 — compute endpoints: pg_try_advisory_xact_lock replaces SELECT-then-INSERT pre-check

- **Lesson:** When replacing a SELECT-then-INSERT race-prone pattern with a transaction-scoped advisory lock, the WHOLE compute work has to live inside the same transaction — otherwise the lock releases the moment any intermediate `conn.commit()` fires. This forces a structural shift: the old pattern committed the `in_progress` row early so external observers could see the work in flight; the new pattern only commits the `completed` row at the very end, and failures roll back the entire transaction (no `failed` row persists). Module-level docstrings that described the old "marked 'failed' on rollback" behaviour become stale and need updating in the same diff. The status table changes meaning: from "in-flight + final outcomes" to "completed runs only". Anyone relying on early in-progress visibility (status endpoints, dashboards) must instead query `pg_locks`.
- **Surprise:** Both the bead spec's example SQL AND my first cut wrote the `except Exception as exc:` block, refactored to `except Exception:` (no binding), and left a stray `raise HTTPException(..., detail=f"...{exc}")` referencing the now-undefined `exc`. The dead-line wasn't reachable (preceding unconditional raise), so no linter-free runtime would surface it — but the code-review subagent caught it as F821 undefined-name in a hypothetical-future scenario where someone removes the preceding raise. **Pattern:** whenever an `except As name:` clause loses its `as name`, immediately scan the rest of the except body for surviving references to `name`. Editor multi-cursor or grep-and-replace is the safer path.
- **Pattern noted:** Postgres `pg_try_advisory_xact_lock(<integer>)` has ONE namespace across all single-argument callers — int4 widens to bigint via implicit cast. Writing "different namespace" in a comment because one caller uses `hashtext('text')` (int4) and another uses literal bigints is technically wrong; only the *values* differ, not the namespaces. The locks.py module now documents this explicitly with the int4 range upper-bound guarantee for the hashtext-derived caller vs the small bigints 1001-1003. Future bigint callers MUST stay outside int4 range or compute the hashtext literal to prove non-collision.
- **Pattern noted (carry-forward):** Three nearly-identical handler bodies (topology / clustering / drift) all wrap the same lock-acquire pattern with only the lock key constant differing. Extraction to a helper (e.g. `with acquire_compute_lock(conn, 'topology'):`) is tempting but premature — the bead's locked spec didn't call for it, and the inline shape keeps the lock-key-to-endpoint mapping explicit at every site. If a fourth caller lands in this style, extract then.

### nmemo-2yv.29 — createFact dedup branch wrapped in db.transaction for audit atomicity

- **Lesson:** When a mutation primitive already has a tx-aware sibling pattern (here: four other paths in `facts.ts` already wrap UPDATE + recordFactChange in `db.transaction(async (tx) => {...})` and thread `tx`), the cheapest test for "is this branch broken?" is to ask whether it follows the sibling shape. The dedup-and-corroborate branch was the odd one out. The fix is mechanical once the pattern is established; the discovery is what costs. **General rule:** when reviewing service-layer mutations for atomicity gaps, list every mutation path in the file and check whether each one wraps its audit emission. The non-wrapped one is the bug.
- **Pattern noted:** The bead's locked spec spelled out the exact code shape (`await db.transaction(async (tx) => { ... await recordFactChange({ ..., tx }); });`) — and the implementation took 2 lines + a comment. The locked spec being concrete made this a fast bead; vague specs would have invited drift. Worth noting that beads where the Decision section includes a *code snippet* are the cheapest to drive.
- **Pattern noted:** The CHECK-violation test pattern (`actor: 'no_such_actor' as any` flowing through to a raw-SQL INSERT that hits the CHECK constraint at runtime) is a clean way to force a downstream-only failure mode. TypeScript erasure means the runtime sees a string the DB rejects, exercising the rollback path without contriving a network blip or DB mock. Reusable for any other tx-wrapped audit row that has a CHECK constraint downstream.

### nmemo-2yv.61 — reconciliation agent pipeline auto-trigger (T12 design-contract drift)

- **Lesson:** When a "documented contract" (here: the reconciliation_agent.py system prompt's "You run AFTER extraction" assertion + RECONCILIATION_AGENT_REVIEW.md's "fire-and-forget at pipeline.ts line 188" description) quietly falls out of the code over a release cycle, the doc becomes a liar but the system keeps shipping. Audit pattern: grep the doc's claimed call sites against the current code at every doc touch, and fail loudly when the line/symbol referenced no longer exists. The bead-finding process caught it here; a CI lint that diff-checks docs vs code references would have caught it earlier.
- **Surprise:** The bead's locked spec said gate on `status IN ('staging', 'candidate')` — broader than the existing /api/reconcile handler which only fires on `'candidate'`. Initial implementation copied the handler's narrower gate. Re-reading the bead spec caught the deviation. **Pattern:** when the bead spec and the existing manual code path disagree on a parameter (here: status enum scope), the bead's locked spec wins by definition — but always re-read the spec after looking at the existing code, since the existing code biases the implementation.
- **Pattern noted:** Wall-clock cooldown gating via a module-level timestamp is simpler than counter-based cadence (the gardener's pattern) when the rate-limit metric is "minimum N minutes between calls" rather than "every N events". For the reconciliation agent's case (LLM-bound, expensive) wall-clock makes more sense than counter; for the gardener (cheap periodic sweep) counter is fine. The two co-exist in pipeline.ts and the test files; both patterns are now blessed.
- **Pattern noted (carry-forward, deferred):** Code-review surfaced a theoretical read-then-write race on the cooldown timestamp — two concurrent ingest pipelines could both observe cooldown=elapsed and both proceed to invoke the agent. Bounded by the serial drainQueue() pattern in production; remains theoretical for direct ingest()/extract() callers. Belt-and-braces fix would be a synchronous-write before the SELECT, but that worsens the no_pending_work case (each empty check would consume the cooldown). Worth filing as a follow-up only if multi-worker deployment ever lands.

### nmemo-2yv.124 — Delete causal-mcp.ts + dead CAUSAL_AGENT_TOOLS export

- **Lesson:** Pure-deletion beads (drop a dead file + stale exports + outdated comments referencing them) compress the full /code-review + /simplify pipeline into a single grep-pass: if every reference to the deleted symbols is gone from production source, the code-review verdict is "clean by construction" and /simplify has nothing to simplify. Pattern: when a bead is a clean delete, the acceptance bullets typically encode the review (zero-hit grep checks across the codebase) and there is no test surface to add. Don't over-process — three Edits and a grep give the same coverage as a 5-angle review.
- **Pattern noted:** A `/** @deprecated */ export const X = []` empty-array placeholder is worse than no export. If anything later imports `X` (refactor, search-and-replace), the consumer runs with an empty tool array and the agent silently misbehaves. The right shape for a deprecated symbol is *removal*, not *zero-out + deprecate*. Apply this lesson the next time we deprecate an exported collection: delete, don't empty.

### nmemo-2yv.101 — patternImpact.nodeType honesty fix (lying cast → honest union extension)

- **Lesson:** When a value gets force-cast through a TS type with an inline `// smell comment` apologising for it (here: `nodeType: 'fact' as ImpactNodeType, // patterns aren't a graph node type — represented as fact-class for the report`), the cleanest fix is almost always to extend the type to admit the honest value, not to find clever ways to suppress the cast. Two-line type union extension + cast removal + matching test + doc — the whole bead is mechanical. The smell comment IS the to-do flag pointing to the right fix.
- **Pattern noted:** Acceptance bullets that ask for a "no `as X` cast, no smell comment" check are a high-signal acceptance shape — they're auditable by grep alone, and they encode an invariant that survives future refactors. Worth replicating: when removing a lying cast, the acceptance bullet should explicitly forbid both the cast AND the apology comment so neither can sneak back in.

### nmemo-2yv.111 — Compose platform /health with Qdrant + ml-services (G1 health composition)

- **Lesson:** When the bead body cites file paths that have since moved (here: `src/ml-client.ts` / `src/qdrant.ts` are actually at `src/services/ml-client.ts` / `src/services/qdrant.ts` since some prior refactor), the *substantive* premise — dead exports, DB-only handler, the helpers' line numbers — can still be intact. The premise verification step is about symbols + behaviour, not paths. Don't halt for stale paths if the symbols and behaviour match; log the drift in the bead notes and proceed. The Acceptance section's *behavioural* bullets (response shape, key presence, AND-of-N logic) are the binding contract — they don't depend on paths.
- **Pattern noted:** When composing N fail-safe probes into one composite endpoint, AND-of-N + per-key reporting in a single `Promise.all` destructure is the minimum-line shape (7 lines for 3 probes). This only works because each probe is *guaranteed* fail-safe — `try { ... } catch { return false }` — so `Promise.all` cannot reject. Before composing, verify each helper's catch-and-return-boolean shape; one helper that throws turns the composite endpoint into a 500-spewing liability. The three helpers here (`checkDatabaseHealth`, `ml.health`, `checkQdrantHealth`) all share the same fail-safe shape. Worth adopting as a convention: any function named `check*Health` or `*.health()` MUST be try/catch → boolean. A future lint could enforce it.
- **Pattern noted (acceptance):** Bead author explicitly classified bullet 4 ("Hit /health while Qdrant is down returns degraded") as "(manual test on stop/start)" — operator infrastructure-shuffle the skill should NOT execute mid-cycle. Lesson: when a bead author parenthesises a bullet as `(manual test)`, treat that as a deferral, satisfied by the code-construction of the surrounding bullets. The skill's halt-on-bullet rule has an implicit "unless the bead author flagged it as manual" carve-out.

### nmemo-2yv.104 — Strip dead `invalidate` / `weaken` hypothetical modes (T13 half-built API)

- **Lesson:** Type-narrow beads have two non-obvious failure modes that a confident first pass will miss: (1) inline string-union casts that bypass the type alias do NOT auto-narrow with the alias. Here `index.ts:594` carried `hypothetical as 'expire' | 'invalidate' | 'weaken' | undefined` — frozen at edit time, untouched by the type narrow. Same pattern as .101's lying cast. The fix discipline: after any type-narrow, grep for the deleted string literals (`grep -n "'invalidate'\|'weaken'"`) AND for the OLD union shape verbatim. Anything that hits in production code is a missed edit. (2) The Edit tool's `replace_all: false` default silently fails when the old_string matches multiple times — it returns "Found N matches" as an error, makes ZERO edits, and the missing edit is invisible unless you re-grep. Treat any "Found N matches" error as a halt-and-investigate signal, not a noise log line.
- **Pattern noted:** `/code-review` is not optional even on "shrink-the-API" beads that look clean by construction. The review pass caught both gaps that I'd shipped otherwise. The cost is ~2 minutes; the alternative is silently lying types post-merge. Never skip the review just because the bead's Scoped fix reads as mechanical.
- **Pattern noted:** The cleanest downstream cast pattern for narrowed TS string-literal unions is `as TheAlias | undefined` (here: `as HypotheticalAction | undefined`, the shape used by `causal-agent.ts:1912`). NOT `as 'lit1' | 'lit2' | ... | undefined`. The alias future-proofs the narrowing — if the union later widens or narrows again, every call site moves automatically; the inline list is dead-frozen at edit time and turns into the next .104. The trade-off is one extra import — pay it. (`index.ts:594` does an inline `as 'expire' | undefined` rather than importing `HypotheticalAction` — defensible only because the union is now 1-wide and any future widening would force a re-edit anyway. The judgement is "if I expect this union to grow again, use the alias; if it's terminal, inline is fine." This union is terminal per the bead's Decision section.)
- **Surprise (cross-project):** Honest accounting — the bead's "Scoped fix" called out four code sites (impact.ts, index.ts validation, causal-agent.ts enum, blast-radius.test.ts). The two gaps I needed code-review to find were NOT in those four — they were (a) a downstream cast at `index.ts:594` (10 lines after the validation block the bead pointed at), and (b) a second occurrence of the union in doc 15 the bead body's line-reference (`118-122`) didn't enumerate. Lesson: even with a detailed Scoped fix, run `grep` for the OLD shape across the WHOLE repo (not just the bead-cited files) before declaring APPLIED. The "found 0 hits" output is the contract.

### nmemo-2yv.121 — Wire ensureCollections + add Qdrant dim validation (T11 silent-drift cure)

- **Lesson:** For Qdrant collection-config narrowing, the `VectorsConfig` openapi schema is a union of `VectorParams` (single-vector — has `.size: number`) and `{ [key: string]: VectorParams | undefined }` (named-vectors record — index lookup, returns `VectorParams | undefined`). Naively accessing `.size` on the union produces `number | VectorParams | undefined`. The clean narrow is `vectorsConfig && 'size' in vectorsConfig && typeof vectorsConfig.size === 'number'` — `'size' in` narrows to members that admit the key (TS treats index signatures as admitting any string key, so this alone doesn't narrow away the record branch), then `typeof === 'number'` narrows to the `VectorParams` branch. Both checks together produce a `number` in the true branch. When the narrow fails (e.g. response shape changed in a future qdrant-client release), `actualSize` is `undefined`, the mismatch throw still fires (with `size=undefined` in the message), and the operator still gets the remediation hint. Defence in depth costs ~one line.
- **Pattern noted:** Top-level `await` in ES modules (target ES2022 + module ESNext) lets a startup probe block process start. The pattern at `index.ts:1657-1660` is now: `if (!process.env.VITEST) { await ensureCollections(); serve(...); }` — the VITEST guard means endpoint tests calling `app.request()` get to bypass the probe at module load. Confirmed via decay-endpoint test: imports `app` from `./index.js`, sets `VITEST='true'` via Vitest, ensureCollections is skipped, test passes. This is the canonical fail-fast-at-boot shape going forward; future startup validators (e.g. .132) should compose into the same VITEST-guarded block.
- **Pattern noted:** For "shipped first / picked up later" cross-bead test dependencies (here: .121 ships first, .114 inherits the responsibility to add the dim-mismatch test case), drop a forward-reference into the downstream bead via `bd update --append-notes` *at the time of shipping*. The downstream bead's history then carries the test spec — when the bead gets claimed, the implementer reads the spec inline rather than having to re-derive it from the cross-references. Cheap insurance against test-coverage gaps falling through the cycle.

### nmemo-2yv.99 — pattern_member severity tiered by edges_outside_root (Review #11/F1)

- **Lesson:** Postgres `COUNT(...)` returns `bigint`, and postgres.js delivers `bigint` over the wire as a JS string. Tests asserting `.toBe(2)` on a `COUNT`-derived field fail with `Received: "2"`. Fix at the SQL boundary with `COUNT(...) ::integer AS field_name` rather than `Number()`-converting in the JS layer — keeps the type contract honest on the row shape. The old `edge_count` field in `findPatternImpact` had the same latent bug; it only stayed hidden because the field was interpolated into a string via the `reasoning` template (string coercion masked the type drift). When introducing a new COUNT-derived numeric field that's read directly, cast to `::integer` at write time.
- **Pattern noted:** When extending a severity rule from constant→tiered, the existing test typically asserted the constant outcome with a fixture shape that matches *one* of the new tiers (almost always the most restrictive — here: orphan, because the prior code's "low" verdict was applied to a single-edge pattern that is actually an orphan under the new rule). Rename the existing test to name the new tier explicitly (`orphan tier — pattern with no edges outside root scores high`) and flip its assertion; add the other tier tests alongside. Reusing the existing fixture for the orphan case is faithful to the prior code's seed; the new tests get their own fixture shapes.
- **Pattern noted (SQL):** Replacing a WHERE-side filter with HAVING + FILTER conditional aggregation lets one query carry both "rows matching predicate X" AND "rows matching predicate Y" without subqueries or joins. The pattern: `COUNT(...) FILTER (WHERE predicate_X)` for selected counts; `HAVING COUNT(...) FILTER (WHERE predicate_X) > 0` for row-level filtering. Cleaner than wrapping the whole thing in a CTE or running two queries. The HAVING expression duplicates the FILTER predicate — SQL doesn't have a clean alias for FILTER aggregates, so the duplication is irreducible without a CTE.

### nmemo-2yv.100 — transitive depth=1 severity tiered by other_causes_count (Review #11/F2)

- **Lesson:** When `scoreSeverity` needs a second batched query (here: `other_causes_count` for depth-1 effects, after the existing citation-edge batch), thread the dependency into `scoreSeverity`'s ctx rather than recomputing or routing through closure. Adding `rootEventIds?: string[]` to ctx + passing it from the orchestrator (which already has `rootEventIds` in scope from `resolveRootEvents`) is one extra line at the call site and keeps `scoreSeverity` self-contained. Avoid plumbing rootEventIds through ImpactNode itself — that's data leakage; the ctx is the right home.
- **Pattern noted:** Two consecutive batched-query blocks in `scoreSeverity` (one for citation sole-evidence, one for depth-1 other-causes) suggest a future refactor: a single `precomputeSeverityInputs(nodes, ctx)` step that runs both queries in `Promise.all` and returns the Maps. Not worth doing today — only two blocks — but if a third tier-by-context query lands, extract. The .99 lesson about `::integer` casts on COUNT applies here too: cast at the SQL boundary so `Number()` calls aren't needed in JS.

### nmemo-2yv.116 — pi-bridge timeout race: hoist session, abort+dispose on timeout

- **Lesson:** When a `Promise.race([resourcePromise, timeoutPromise])` shape is fixed by giving the timeout branch the ability to clean up the resource, the resource creation must hoist OUT of the agentPromise IIFE — both branches need a reference. The pattern: create resource → set up timer-resolving timeoutPromise that closes over the resource → start agentPromise that uses the resource (still in IIFE for its event-handling closure) → race. The dispose path needs four discrete states tracked: `timedOut` flag (so the agent's finally can skip double-dispose), `timeoutHandle` (so the agent's finally can clearTimeout on normal completion), the timeout chain's own abort-then-dispose chain (fire-and-forget via `void session.abort().finally(...)` so the caller's response isn't blocked on cleanup), and gating dispose on `!timedOut` in the agent's finally.
- **Pattern noted:** Post-timeout closure-variable mutations don't leak into the already-resolved response — primitives are captured by value at resolve-time, and the subscribe handler's `cost = { ... }` REASSIGNS a new object reference rather than mutating in place. This is a fragile assumption: if a future event handler does `cost.input_tokens = N` (mutation) instead of `cost = { ... }` (reassignment), the timeout response could be retroactively edited. Worth a sentinel comment near the resolve site if the pattern grows hairier. For now: the reassignment shape is the implicit invariant.
- **Pattern noted (test deferral):** When a bead's acceptance bullet calls for a unit test that COUPLES across two beads (.116 + .117/F8) and one of those beads is halted, defer the test to the live one with a forward-reference note (same pattern as .121 → .114). The deferred test's spec lives in BOTH bead histories so whoever picks up the live bead has it in context. Avoids mocking infrastructure for a single isolated test; keeps the deferred bead's scope tighter.

### nmemo-2yv.92 — cross_cluster_runs telemetry table: record-then-work pattern (T9 cure)

- **Lesson:** When wrapping `await db.transaction(...)` to capture errors for post-tx side effects (here: UPDATE a telemetry row to status='error'), prefer `.catch()` + a boolean `errored` flag over `try { } catch { }`. Reasons: (a) saves a +2 indent on the entire closure body — for cross-cluster-generator.ts that's ~160 lines of re-indent for zero behaviour change, a churn-heavy diff that the reviewer must visually filter through; (b) the boolean flag defends against `throw null` / `throw undefined` / `throw 0` from third-party code that a `mainError !== null` sentinel would miss-route into the success branch. Pattern: `let errored = false; let mainError: unknown; await tx(...).catch((e) => { mainError = e; errored = true; }); if (errored) { ...UPDATE+rethrow... }`. The closure's exit paths set a `result` variable that the success branch reads via a non-null assertion (`result!`) since TS can't narrow across the `.catch()` boundary — the invariant is enforced structurally by the closure paths, not by a defensive fallback literal.
- **Lesson:** The `record-then-work` telemetry pattern (INSERT 'running' row in a short SEPARATE transaction BEFORE opening the main advisory-lock tx, UPDATE after the main tx resolves) is strictly better than the inline-write pattern used by sibling compute runs (topology_compute_runs / clustering_compute_runs) because the row survives main-tx rollback — error paths still leave a forensic record. Worth lifting to siblings in a future bead; doc them as the same shape `Topic [INSERT 'running'] → [main tx work] → [UPDATE 'completed'/'skipped'/'error']`. The separate-tx INSERT is so short that pg connection acquisition dominates its cost; not a hot path.
- **Surprise (test pattern):** For "main tx throws and the function rethrows" tests, the cleanest fault-injection in vitest+postgres is a BEFORE INSERT trigger on the target table that `RAISE EXCEPTION` unconditionally. Scoped, restorable via DROP TRIGGER IF EXISTS, no schema mutation. The orphan-row approach (insert FK-violating entity_id rows in topology/clusters) DOESN'T work because those parent tables themselves have FKs to entities — orphans can't be planted there. Defensive cleanup in `fullReset` (drop trigger + function with IF EXISTS) protects against test-process kills between CREATE and the `finally`-block DROP — without it, a leaked trigger silently breaks every subsequent test that reaches `upsertCandidate`.
- **Pattern noted:** Adding a required field to an existing public interface (`runId: string` on `CandidateGenerationResult`) is the kind of change that breaks downstream callers only if they construct the object directly. Grep `<InterfaceName>` and `<existing-field>: ` is cheap insurance; in this case all 4 construction sites were inside the single source file, so no cross-file churn. If construction sites had been external, the bead would have grown to N+1 files; flagging that boundary up-front in the fix spec is worth ~30 seconds of grep at lock-time.
- **Pattern noted (simplify catches):** /simplify subagent flagged two dead-code clauses left over from the first-pass implementation: (a) a `result ?? {...}` fallback that was unreachable since every closure exit path sets `result`, and (b) a guarded `runId` assignment that was dead since all sites already set it. Both removed without behaviour change. Lesson: when iterating through a closure rewrite, defensive fallbacks at the consumer site accrete as load-bearing-looking dead code; /simplify catches them where /code-review wouldn't (because they don't change behaviour, they just clutter). The CLAUDE.md "don't add fallbacks for impossible scenarios" rule is the right policy — these were violations introduced under iteration pressure, not intentional defence.

### nmemo-2yv.91 — Cross-cluster score weights env-overridable + normalise-with-warn

- **Lesson:** Sum-of-weights checks must use `Math.abs(sum) < EPS` rather than `sum === 0`. Strict equality misses near-zero floating-point cancellation (e.g. `+0.5 + -0.5 + …` summing to `-1e-17`). The original first pass used `sum === 0`; the code-review subagent flagged it as a "bug seed". If an operator types a negative env weight that happens to cancel against another positive one to a tiny non-zero, the normalise branch would then divide each weight by ~0 → astronomical weights → every pair clears the threshold. The `Math.abs(sum) < EPS` form catches both exact-zero and near-zero in one check, and emits a `console.warn` naming the actual sum so the operator sees what their typo produced.
- **Lesson:** Per-invocation env reads (rather than module-load reads) are essential when tests need to override env vars within a single process — `computeWeights()` is called inside `generateCrossClusterCandidates()` rather than at module top, so each test sees its own env state. Vitest isolates env between test files by default; this `beforeEach` cleanup (deleting all 6 `CROSS_CLUSTER_W_*` env vars) is defensive within the file.
- **Surprise (test calibration):** A "lower the threshold to reveal more candidates" test improvement initially turned a passing test into a failing one. With `BRIDGE_SCORE_THRESHOLD=0.1`, pairs near the threshold under default weights surfaced — and under the 2x-cluster normalisation, the centrality weight scales DOWN proportionally, dropping those near-threshold pairs below 0.1. Normalisation is scale-PROPORTIONAL for the relative ordering of pairs strictly above threshold in BOTH runs; it is NOT scale-preserving across the threshold cut. The fix: revert to default threshold and seed pairs that are clearly above it in both runs, so the property under test is "ordering of the COMMON pair-set is preserved", not "every pair surfaces in both runs". The bead's acceptance bullet 7 implicitly assumed this — it took the failing test to make the assumption explicit.
- **Pattern noted (invariant guard):** Whenever a feature's correctness depends on a literal constant invariant (here: defaults sum to 1.0), add a one-line `expect(0.35 + 0.125 + 0.125 + 0.20 + 0.15 + 0.05).toBeCloseTo(1.0, 6)` test. Catches future regressions where someone tweaks a default and breaks the sum — without the guard, the only signal is a `console.warn` on every default invocation in CI logs (cosmetic noise, no behavioural fail). Cheap insurance.

### nmemo-2yv.90 — resolution_reasoning B4 lock (R3 B4 extended one level down)

- **Lesson:** When a partial lock exists (here: `candidate_source = CASE WHEN merge_candidates.candidate_source = 'X' THEN ... END`), the symmetric lock on adjacent columns is rarely automatic. Always grep for the lock-key column (`candidate_source`) across all upsert sites and audit which adjacent columns ride EXCLUDED. The bead caught one (`resolution_reasoning`). Future audit candidates: any column whose value is "source-dependent context" — i.e. the column's value is only meaningful in the context of the row's `candidate_source`. The 3-signal columns (centroid_similarity, memory_overlap, structural_similarity) are not source-dependent (they're NULL on cross-cluster rows, computed on three-signal rows; either way, EXCLUDED is fine because EXCLUDED is NULL on cross-cluster and computed on three-signal — no clobber).
- **Lesson (negative finding):** The bead's acceptance bullet 3 asked whether `graph-meta.ts`'s three-signal upsert also needed the symmetric fix. Grep + read: `graph-meta.ts:235-246` doesn't write `resolution_reasoning` OR `candidate_source` in its ON CONFLICT clause at all — both columns are simply unchanged on collision. So the lock from the cross-cluster side is sufficient; no follow-up bead. Worth documenting the negative finding inline in the bead notes — future readers benefit from seeing the audit was done.
- **Pattern noted:** When extending an existing test rather than writing a new one, name the test to reflect what BOTH original + new assertions cover. Here: 'ON CONFLICT preserves cross_cluster_generator (never downgrades to three_signal_scoring) AND preserves resolution_reasoning (bead nmemo-2yv.90)'. Long but accurate. Future readers grep the test name to find the coverage; a generic 'preservation' rename would lose the source-tag specificity. Tradeoff: long test names are ugly in test runner output; precise test names are debuggable. Choose precision.
- **Surprise (test design):** A first-pass test included a third assertion: "after a second cross-cluster run, the reasoning still equals the first run's reasoning". This is technically true (the CASE makes cross-cluster reasoning sticky against ALL future upserts including cross-cluster re-runs) but trivially passes because identical inputs produce identical reasoning. Removed because the assertion couldn't fail. Lesson: a test assertion that can only pass — never fail — is dead code in the test suite. /simplify-style discipline applies to tests too.

### nmemo-2yv.130 — Enum-vocabulary SSOT canary (RESOLUTION_VALUES)

- **Lesson:** The clean SSOT pattern for an enum used by both Claude tool schemas (mutable `string[]` expected) and TS type guards is `readonly` tuple + `[...TUPLE]` spread at the tool-schema call site. The spread produces a fresh mutable array satisfying the tool-schema type while the readonly tuple narrows the static type elsewhere. Type alias is `typeof TUPLE[number]`. One declaration, two consumers, no codegen.
- **Lesson (migration discipline):** When DB CHECK constraints mirror a TS enum, the migration that ALTERs the CHECK carries a comment naming the SSOT constant: `-- keep in sync with src/services/enums.ts:RESOLUTION_VALUES (bead nmemo-2yv.130)`. The comment is non-load-bearing — it's a discovery hint for future readers, not a runtime check. The bead explicitly rejected codegen (option b) and CI-only lint (option c) — they add build complexity and review surface for the same outcome. Hand-sync with a discovery comment is the right minimum.
- **Pattern noted (parameterised test):** `it.each(TUPLE)('...%s...', async (value) => ...)` is the right shape for a "every value in this SSOT must satisfy the constraint" test. Each value gets its own test name in the runner output (`"merge"`, `"same_as"`, etc.), so a future regression names the offending value directly. Pairs cleanly with the SSOT pattern: the test fails BEFORE the CHECK migration is updated if a value is added to the TS tuple but not the SQL CHECK — exactly the asymmetric-drift case the bead exists to catch.
- **Surprise:** Bead .60 (the canary's parent P1) was already closed (2026-05-25 — migration 018 had already aligned the vocabularies at the layer level). The .130 work then was purely about creating the SSOT module + plumbing — no "fix the silent failure" pressure, just hygiene work. Worth noting in cycle synthesis: when a P1 fix lands BEFORE its enclosing infrastructure bead, the infrastructure bead becomes hygiene-only. This is fine — the infrastructure still pays off when the NEXT enum case arises (.27, .28, .39, .69 still open under T7) — but it changes the urgency calculus.

### nmemo-2yv.127 — Write-tool serialisation moved into shared dispatcher

- **Lesson:** When two transports (Pi bridge + MCP) share a dispatcher, any per-tool execution policy (serialisation, retry, timeout) belongs IN the dispatcher, not duplicated in each transport. The bead's previous shape — `WRITE_TOOLS` set + `isWriteTool` helper in pi-agent-bridge with `executionMode: isWriteTool(...) ? 'sequential' : 'parallel'` — gave the MCP transport's `setRequestHandler(CallToolRequestSchema, ...)` zero serialisation, racing on entity resolution + merge + expiry whenever Claude emitted multiple `tool_use` blocks in one message. Single source of truth at the dispatcher: `let writeQueue: Promise<unknown> = Promise.resolve();` + `if (WRITE_TOOLS.has(toolName)) { writeQueue = (writeQueue.catch(() => undefined)).then(run); return writeQueue; }` — both transports inherit it.
- **Lesson:** Queue-fails-OPEN semantics (`.catch(() => undefined)` on the prior promise before chaining) are the operationally-safe default. Queue-fails-CLOSED would convert a single transient write failure into a denial-of-service on every downstream write in the same process — every patrol cycle would stop after the first hiccup. Fails-open is one extra `.catch(() => undefined)` line for the entire reliability story.
- **Pattern noted:** The bead's Decision section rejected the "add `mutates: boolean` to ToolDefinition" alternative for a specific reason worth recording: per-tool flags spread the policy across 17+ tool defs, making "which tools mutate?" harder to grep, not easier. A single centralised `Set<string>` is greppable in one line; per-tool flags require scanning every tool def. Counter-intuitive — the more "type-safe" design is actually the worse one for code archaeology.
- **Pattern noted (test design):** Proving sequential execution from outside the queue is hard without instrumentation hooks. The three tests here prove the queue's contract pragmatically: (1) two concurrent writes both complete and both rows persist (queue doesn't deadlock or lose work); (2) a failing write does NOT block the subsequent write (fails-open); (3) two concurrent reads both complete (reads aren't in the queue). Skipping strict timestamp-ordering assertions avoids fragility from ms-precision NOW() collisions. The queue's correctness is observable by code review + these three contract tests.
- **Surprise (test invariance):** The existing pi-agent-bridge test at line 62 inlines its own 17-tool WRITE_TOOLS set as a defensive invariant — it doesn't import the production set; it independently asserts those 17 names exist in GRAPH_TOOLS. This was originally to catch "did someone accidentally remove a tool?". After the .127 refactor, the inline set serves a NEW purpose: it catches "did someone accidentally remove a tool from the WRITE_TOOLS set in causal-agent.ts?" — because both must align with GRAPH_TOOLS. A defensive duplicate that started as one safety net and gained a second use case. Lesson: defensive test duplicates with clear purpose are not always violations of DRY — sometimes they're independent checkpoints.

### nmemo-2yv.131 — Compute-trigger registry (doc 32)

- **Lesson:** A registry doc is most valuable when it's small enough to read in one sitting and structured enough that adding a row is mechanical. The five trigger types (post-ingest / DB-reactive / scheduled / threshold-driven / manual) cover every existing compute and provide a forcing-function on new compute design: "which of these five am I? if I can't pick one, my trigger model is unclear." The single-table registry + 4-paragraph taxonomy fits in <250 lines — small enough that nobody can claim "I didn't find the section about my compute". Avoided multi-page subdoc-per-compute structure; that would be longer to maintain than the underlying code.
- **Lesson (placeholder discipline):** When a registry has placeholder entries (`MANUAL ONLY — see <bead>`), they MUST name the implementing bead and what the target trigger condition is. "TODO" entries with no traceback get lost. The forward-references from doc 32 to the five P1 beads (`.61 .71 .72 .84 .85`) mean each bead's implementer sees doc 32 in their search results when they grep for their bead ID, prompting them to update the row in the same PR.
- **Pattern noted:** When the bead's Decision section explicitly rejects a more elaborate alternative (here: YAML manifest + CI lint, TS-side code registry), the doc itself should reference the rejection so future readers don't re-propose the rejected option. Doc 32 §1 directly states "no YAML/JSON manifest, no CI lint, no TS-side code registry; bead .131 Decision section weighed those alternatives and rejected them as adding moving parts ahead of evidence that drift was a real problem." Documentation of "what we decided not to do AND why" prevents drift cycles where the same rejected option resurfaces.

### nmemo-2yv.132 — Startup-validation invariants integration point

- **Lesson:** A validator wrapper that BOTH catches exceptions AND enforces a timeout is the right shape for boundary checks at startup. The pattern: `Promise.race([validator.run(), new Promise((_, reject) => setTimeout(() => reject(new Error('timeout ...')), N_MS))])` inside a try/catch that returns `{ok: false, detail: message}`. This means a validator that throws on unreachable network calls (e.g. ensureCollections throwing on Qdrant dim mismatch) AND a validator that hangs (e.g. ml-services returning a never-resolving promise) BOTH surface as a normal `ValidatorResult` with `ok: false` — no exceptions ever propagate to the aggregator. The aggregator's job becomes trivially "run them all, collect results, report".
- **Lesson:** The aggregator MUST NOT short-circuit on first failure. Operators need to see the full picture — e.g. "Qdrant dim mismatch AND ml-services unreachable AND port collision" — to know whether they have one config mistake or three. The naive `for-of` loop that pushes every result is the right shape; do NOT use `Promise.allSettled` either, because it allows validators to run concurrently, which makes the timing analysis harder when one of them legitimately needs 5s.
- **Pattern noted (test-only export):** `export const _testOnly = { runValidator, VALIDATORS };` is the convention for exposing internals to tests without forcing them into the public API surface. The `_testOnly` prefix makes it greppable as a non-production export ("if I see this in a production file, that's a code smell"). The bead's per-validator unit tests (`ports`, `transport`) reach into `_testOnly.VALIDATORS` to drive individual validators in isolation — much cleaner than trying to mock `ensureCollections` / `ml.health` / `checkGraphMcpHealth` at the module level.
- **Pattern noted (strict-only contract):** The bead explicitly rejected a `STRICT_STARTUP=true|false` env opt-out. "An env-var escape hatch recreates the silent-drift problem this bead is fixing" — once you have two startup modes (strict vs lax), operators can't predict which is live; "did I forget to set STRICT_STARTUP?" becomes a new failure mode. Strict-only is a forcing-function on getting the validators right. Lesson generalises: when adding a safety check, the "are we sure?" knob is itself a footgun.
- **Surprise:** Migration into the `if (!process.env.VITEST)` guard was a one-line replacement (`ensureCollections()` → `validateStartup()` block) — the prior bead `.121` had already wired ensureCollections at this spot. Two beads composing cleanly across two sessions; the `.121` work paid off here. Worth noting in cycle synthesis: infrastructure beads that land FIRST + small make later beads composable.

### nmemo-2yv.126 — MCP health probe: shared env builder + topology round-trip

- **Lesson (real bug caught by mock):** The first-pass test for the JSON-RPC three-message sequence failed with `expected 6 to be 3` — the probe sent 6 messages instead of 3. Root cause: the `proc.stdout.on('data', ...)` handler appends each chunk to a cumulative `stdout` string and then iterates `stdout.split('\n')` every time data arrives. After three data events, the id=1 line is in the buffer 3 times → the "send tools/list on id=1" branch fires 3 times → 3 duplicate tools/list sends. Same pattern for the id=2 branch. Production code had been silently doing this; the old test (which only had a 2-stage flow and resolved after id=2, setting `resolved=true`) didn't expose it because once resolved, subsequent re-fires were no-ops via the `finish` guard. Adding a 3-stage flow exposed the duplication. Fix: `let toolsListSent = false; let topologyCallSent = false;` guards on the dispatch sites. Lesson: streaming-buffer protocol code that doesn't track "already processed" is fragile against multi-stage flows. Either prune `stdout` as lines are consumed, OR track per-stage send flags.
- **Lesson (test placement):** The bead suggested putting subprocess-mocked tests in `causal-mcp.test.ts`. I put them in a NEW file `graph-mcp-health.test.ts` because `vi.mock('node:child_process')` is module-level — it affects every test in the file. The existing `causal-mcp.test.ts` and `causal-integration.test.ts` deliberately spawn the REAL subprocess to integration-test the probe. Co-locating the mocked tests would have either (a) broken those existing tests by replacing real spawn with a mock, or (b) required selective unmocking that complicates the test file. Separate file is the cleaner shape — different boundary, different test file.
- **Lesson (JSON-RPC error semantics):** Two distinct failure modes that look superficially similar:
  - **Tool-level error** (`result.isError: true`): the tool ran but failed at the application layer. MCP→DB path worked, the agent just got a useful error message. `topologyOk: true`.
  - **JSON-RPC protocol error** (`error: {code, message}`): the protocol itself rejected the call (method not found, schema mismatch, parse error). The tool never ran. `topologyOk: false`.
  Treating these as equivalent (the first-pass code did) would mask real protocol-layer bugs (e.g. a rename of `get_graph_topology` that didn't reach the server). The probe MUST distinguish.
- **Pattern noted (shared resolver):** Same as `.123`'s `getGraphMcpScriptPath` pattern — `getMcpEnv(actor)` is a shared resolver that both the writer (`getMcpConfigPath`) and reader (`checkGraphMcpHealth`) call. Drift between the two becomes a compile-time / single-grep signal rather than a silent runtime divergence. Pair this lesson with `.123`'s for a complete "every config that crosses process boundaries needs a shared TS resolver" rule.
- **Surprise (microtask interleaving in tests):** The mocked probe test needs `await Promise.resolve()` between each `emit(proc, ...)` call to let the probe's stdout handler observe each line + dispatch the next request. Without the microtask boundary, all three messages arrive synchronously, the handler runs once, and the test races. `await Promise.resolve()` is the lightweight scheduling primitive — `setTimeout(0)` works too but adds 1ms+ per step. Convention worth keeping: explicit `await Promise.resolve()` after each simulated server response in any mocked-IO test.

### nmemo-2yv.115 — ml-client.ts unit tests (retry / timeout / fence-strip / error-wrap)

- **Lesson (fake-timers + backoff):** When testing retry logic with backoff sleeps, `vi.useFakeTimers() + vi.advanceTimersByTimeAsync(N)` is essential — real `setTimeout` waits would balloon the test from ~10ms to ~5s for a 3-attempt retry. The pattern: spy on fetch, queue per-attempt responses with `.mockResolvedValueOnce(...)`, kick off the promise, then `advanceTimersByTimeAsync(2_000)` to walk through the [500, 1000]ms backoff steps before awaiting the promise. Without fake timers, the test suite is unviable for retry coverage.
- **Lesson (bead acceptance verification):** The bead claimed "manually dropping the conditional spread in extractEntities causes test 7 to fail" — but `JSON.stringify({text, valid_types: undefined})` drops the `undefined` key entirely, producing identical wire output to the spread version. The regression is observable only at the TypeScript object-construction level, not at the serialised wire level. Test 7 was kept (documents intent) but the bead's regression claim was inaccurate. Lesson: when authoring a "manually regress X causes test Y to fail" acceptance bullet, mentally walk the regression through to the assertion. JSON's undefined-stripping behaviour bites here.
- **Pattern noted (DOMException AbortError):** `fetch` rejection on `AbortController.abort()` produces a `DOMException` with `.name === 'AbortError'`. The test mocks this with `new DOMException('The operation was aborted.', 'AbortError')` — works in modern Node (≥17 where DOMException is global). Older Node would need a stub. The production code's check `error instanceof DOMException && error.name === 'AbortError'` correctly distinguishes this from generic network errors so the retry policy excludes timeouts.
- **Pattern noted (helper-builder for Response mocks):** A `mockResponse({ok, status, body, text})` helper that returns an object with the minimum `Response` surface (`.ok`, `.status`, `.statusText`, `async json()`, `async text()`) keeps the tests readable. A full `Response` mock would carry headers, blob, formData, etc. — none of which mlFetch reads. Mock the smallest surface that satisfies the contract.
