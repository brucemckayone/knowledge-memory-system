# Triage — suspected stale / decided-against cluster (2026-09-22)

Branch `feat/single-graph-retrieval` @ `bcb95a7`. Read-only verification: no source edited, DB SELECT only.
Substrate queried: `cognitive_test` via `docker exec -i nmemo-postgres-1 psql -U cognitive -d cognitive_test`.

Claim labels used below: **[V]** verified by my own reading / query / reproduction · **[C]** claimed by the
bead or a doc, not independently re-derived · **[I]** my inference.

## Verdict table

| bead | verdict | one-line evidence |
|---|---|---|
| **nmemo-w2p** | **DECIDED-AGAINST** | Script exists, `fact_predicates` = 48 rows / **0** embeddings, fold is a graceful no-op (`predicate-resolve.ts:124-129`); doc 41 measured the fold at 3.7% vs a 60% bar with merge precision 0.43, and the bead's own comment says "do NOT run the backfill". **But OFF is not enforced — there is no flag.** |
| **nmemo-xhn** | **STILL-BROKEN (latent)** | Fast path `if base in canonical_set` still pre-empts the type-keyed scorer at `ml-services/app/resolve_predicate.py:119`; no union-find anywhere; **reproduced 8 strings / 110 facts exactly** from the banked replay. Dormant only because the fold is inert — it is the reason w2p must stay un-run. |
| **nmemo-hxg** | **FIXED** | `ca60276` retired AGE from the read path. `graph.ts` now has **zero** `catch`, zero `return []` swallows and zero `cypher(`; `findConnectedEntities:358-388` delegates to the recursive-CTE `traverseFromEntities`. |
| **nmemo-w6o** | **STILL-BROKEN** (experiment-tool scope) | `concept-multihop.ts:79` `?? 100_000`, `:169` `LIMIT maxPairs+1`, `:173-178` warn-and-slice — unchanged. No live read path, **but `multihop-identity-check.ts:24,42` still calls it with no `maxPairs`**, so one real caller runs on the unsafe default. |
| **nmemo-0o6** | **STILL-BROKEN** | `multihop-identity-check.ts:37` `identical = onlyShipped.length === 0 && onlyMultihop.length === 0` — no non-empty guard; file has exactly one commit (`a300c7a`). Both destructive suites confirmed: `audit-ledger.test.ts:49`, `audit-pass.test.ts:56`. |
| **nmemo-jcj** | **STILL-BROKEN — and materially worse** | **43** colliding doc numbers across 4 trees. "doc 39" now has **4** candidates (bead said 3), "doc 38" 4, "doc 23" **8**, plus a collision the bead missed: **two files numbered 35 inside `cross-corpus-audit/`**. The bead's own mitigation ("new work under `single-graph/`") is what made it worse. |
| **nmemo-7ra** | **PARTIAL — premise survives, experiment blocked** | Mechanism is **live** (`index.ts:1446-1515` gates `recallViaGraph` on `flatRetrievalFailed` in `/api/reason/query`; also `causal-agent.ts:1915`). Docs 22/47 killed traversal in a *different* regime, not this one. Blocked by substrate: `source_memory_id` NULL on **343,018/343,018** facts and `fact_units` = **0** rows. |
| **nmemo-eue** | **DECIDED-AGAINST (parked) — NOT completed by docs 42/43** | Docs 42/43 are Path B, **retrieval-only, explicitly Claude-free** ("no LLM judge, no extraction, no DB", doc 43:82), n=150/121 subsets, and **exclude abstention** — which is the one criterion they would have had to satisfy. Doc 00 §160: "Parked by user decision." |

**Closable:** hxg (code changed), w2p (project decided), eue (user parked).
**Not closable on staleness:** xhn, 0o6, jcj, w6o. **Re-scope, do not close:** 7ra.

---

## 1. nmemo-w2p — backfill never run / fold inert — **DECIDED-AGAINST**

Every factual element of the bead is still true **[V]**:

- `platform/scripts/backfill-predicate-embeddings.ts` exists.
- `select count(*), count(embedding) from fact_predicates` → **48 rows, 0 embeddings** (27 `canonical`, 21 `rejected`).
- `loadPredicateCandidates` filters `WHERE embedding IS NOT NULL` (`predicate-resolve.ts:36-45`), so
  `canonicalizeStagedPredicates` takes its documented no-op branch at `predicate-resolve.ts:124-129`
  (`stats.deferred = facts.length`). The fold has still never attempted a match.
- The backfill is referenced in **docs only** — never in `package.json`, a Makefile, a migration or startup.
  Docs 38/39 still instruct running it ("**Fix: run …** (Bead P0)", `39-known-truths.md:48`).

The *action* the bead asks for is forbidden **[C, doc 41 + bead comment]**: 2,240 → 2,157 distinct predicates
= **3.7%** against a pre-registered PASS bar of 60% / PARTIAL 30%; blind-adversary merge precision **0.43** with
a 28.4% indefensible floor (negations, polarity flips, a direction inverse). `CLAUDE.md` records
"the predicate fold stays OFF" and "do NOT run the predicate backfill". Close as **decided-against**, not stale.

### Surprise worth a replacement bead

**"OFF" is not enforced anywhere. [V]** There is no feature flag — `grep` for `predicate_fold` / `predicateFold`
/ `enablePredicateFold` across `platform/src`, `ml-services` and `.env*` returns **nothing** outside a replay
tool's filename. `promotion.ts:714` calls `canonicalizeStagedPredicates` **unconditionally** on every promote.
The fold is off purely because 48 registry rows happen to have `NULL` embedding, and two architecture docs
still instruct a reader to run the one script that flips it on. There is also no startup check making an empty
candidate set loud (the bead's own process finding) — `startup-validation.ts` has no `fact_predicates` mention.
**[I]** A one-line env guard, or deleting/renaming the backfill script, would convert a data accident into a
recorded decision. This is the residual that should outlive w2p.

## 2. nmemo-xhn — fold is not a function of the predicate string — **STILL-BROKEN (latent)**

**Mechanism still present [V].** The bead cites `predicate-resolve.ts`; the actual site is
`ml-services/app/resolve_predicate.py:115-130`:

```python
base, temporal_hint = normalize_tense(request.predicate, alias_to_canonical, canonical_set)
# Fast path: ...
if base in canonical_set:
    return ResolveResponse(decision="merge", canonical=base, base=base, ... score=1.0,
                           signals={"combined": 1.0, "exact_match": True}, top=[])
```

The fast path keys on the **string** and returns before any scoring, while the TS cache keys on the **triple**
(`predicate-resolve.ts:140`: key = `${f.predicate} ${subjectType ?? ''} ${objectType ?? ''}`). So once a
string is minted under one type pair it self-resolves for every later pair, pre-empting the merge the scorer
would have made. Unchanged since `a1a07d5` / `1cc1483` (`git log -- ml-services/app/resolve_predicate.py` = 2
commits, neither touching the fast path). No union-find over the redirect graph exists: the only `Union-find`
hits in the tree are `causal-agent.ts:2765` (components) and `promotion-plan.ts:549` (entity prefixes).

**Numbers reproduced exactly [V].** Re-derived from the banked replay artifacts
(`docs/architecture/cross-corpus-audit/predicate-fold-artifacts/{resolved-map.json,input-facts.ndjson}`,
5,714 facts) by joining each fact's raw predicate to its resolved canonical:

```
strings with >1 canonical: 8
  addresses_challenge        -> {addresses_challenge: 20, addresses_problem: 1}
  code_available_at          -> {code_available_at: 7, source_code_available_at: 1}
  enables_capability         -> {enables_capability: 46, has_capability: 5}
  evaluated_on_dataset_count -> {evaluated_on_dataset_count: 1, evaluated_dataset_count: 1}
  extends                    -> {extends: 9, extends_concept: 1}
  improves_upon              -> {improves_upon: 7, improves_over: 1}
  suffers_from               -> {suffers_from_problem: 1, suffers_from: 2}
  supports_modality          -> {supports_modality_collaboration: 3, supports_modality: 4}
all facts on multi-canonical strings: 110
```

8 strings, 110 facts — bit-for-bit the bead's claim. (Note the split is lopsided: only **14** of the 110 sit on
the non-self canonical. **[I]** The defect is real and confluence-breaking, but its measured blast radius in
that run was 14 facts, not 110.)

**Does it matter given w2p?** Not today **[I]**: with zero registry embeddings, `canonicalizeStagedPredicates`
returns before it ever calls `/resolve-predicate`, so the non-confluence cannot fire in production. It fires the
instant anyone runs the backfill. Keep open as the **gate** on w2p (or fold the two into one "predicate fold:
off by accident, broken if switched on" bead) — do not close it as stale.

## 3. nmemo-hxg — graph-anchored fallback returns [] on unsynced AGE — **FIXED**

The described code no longer exists **[V]**. Commit **`ca60276` (2026-08-31)** "refactor(graph): retire Apache
AGE from the read path; traverse public.facts instead" rewrote it:

- `grep -n "catch\|return \[\]\|ag_catalog\|age_graph\|cypher" platform/src/services/graph.ts` → **one** hit,
  `graph.ts:124` `if (rootIds.length === 0) return [];` (an empty-input guard, not a swallowed error). There is
  **no try/catch** in the file at all, so the `graph.ts:270-273` catch-and-return-`[]` the bead cites is gone.
- `findConnectedEntities` (`graph.ts:358-388`) now validates the UUID, maps the AGE-convention
  `relationshipType` back to a predicate, and delegates to `traverseFromEntities`
  (`graph.ts:101-160`) — a `WITH RECURSIVE reach(node, hops)` CTE over `public.facts` with `LIVE_FACT`
  expiry filtering. Zero AGE dependency, so "AGE lags" is not a reachable state for this path.
- `graph-fallback.ts:34,199` imports and calls `traverseFromEntities` directly; `findConnectedEntities` no
  longer appears in it (`:191-193` is the comment recording the removal).
- The only surviving `cypher(` in `platform/src` outside tests is `index.ts:1358`
  (`MATCH (n) DETACH DELETE n`) — a reset/write path, not a read path.

**Timing note [V]:** `appendices/02-beads-corpus.md:85` still describes hxg as live, and that appendix
(`4f9452a`) landed the **same day** as `ca60276`. The survey and the fix raced; the bead was never updated.

`traverseFromEntities` *does* still fail-soft in one sense (`corpusId` defaults to `null` = all corpora), but
the author deliberately documented that choice at `graph.ts:105-116` precisely to avoid a new silent-`[]`, so
it is not a residual of this bead.

## 4. nmemo-w6o — `recallMultiHopConcepts` silent truncation — **STILL-BROKEN** (experiment-tool scope)

Defect verified present and unchanged **[V]**:

- `concept-multihop.ts:79` — `const maxPairs = opts.maxPairs ?? 100_000;`
- `concept-multihop.ts:169` — `LIMIT ${maxPairs + 1}`
- `concept-multihop.ts:173-178` — `if (r.length > maxPairs) console.warn(...)` then `r.slice(0, maxPairs)`.
  No `truncated` field on `MultiHopPair`, no throw. A stderr warning is still not a return value.

**Reachability [V].** Not in any live read path: no reference in `index.ts` (checked with `grep -a`, per the
NUL-byte trap), no MCP tool, no service caller. The only two callers are experiment tools:

- `platform/src/test/tools/multihop-score.ts:192,233` — **passes `maxPairs: MAX_PAIRS` explicitly** (the
  `a4ea2ee` scorer fix the bead describes, with the adversary finding recorded in its own header at `:26-27`).
- `platform/src/test/tools/multihop-identity-check.ts:24,42` — **does NOT pass `maxPairs`.** Still on the
  unsafe default.

So the premise is not stale, and the surviving caller is the unsafe one. The concept layer's negative closure
(docs 28-32) removes the *product* value of a fix, not the defect. **[I]** This compounds with nmemo-0o6 below:
the identity check both truncates at 100k and passes vacuously on empty sets — the doc-35 true sizes were
hops=1 **137,922** pairs **[C, bead/appendix]**, i.e. above the cap, so the correctness anchor for the primitive
is itself running on truncated inputs. Suggest merging w6o into 0o6 as one "the multi-hop correctness anchor is
not trustworthy" item at P3, rather than closing either.

## 5. nmemo-0o6 — vacuous PASS in `multihop-identity-check.ts` — **STILL-BROKEN**

Verified verbatim **[V]**, `platform/src/test/tools/multihop-identity-check.ts:30-40`:

```ts
const onlyShipped = [...S].filter((k) => !M.has(k));
const onlyMultihop = [...M].filter((k) => !S.has(k));
...
const identical = onlyShipped.length === 0 && onlyMultihop.length === 0;
console.log(identical ? '  PASS — hops=0 reduces exactly to the shipped JOIN' : '  FAIL — ...');
```

`S.size === 0 && M.size === 0` satisfies it. No non-empty precondition, no expected-count assertion.
`git log -- platform/src/test/tools/multihop-identity-check.ts` returns **exactly one commit** (`a300c7a`,
the original) — the one-line guard was never added, confirming the bead's 2026-08-31 audit finding.

Both destructive suites confirmed **[V]**: `audit-ledger.test.ts:49` and `audit-pass.test.ts:56` both run
an **unscoped** `DELETE FROM public.bridge_edges`. (`concept-extraction.test.ts:56` and
`concept-recall.test.ts:59` are correctly corpus-scoped, for contrast — and `audit-pass.test.ts:18` documents
the unscoped delete as intentional: *"so other suites can freely DELETE bridge_edges"*.)

Substrate is intact **right now [V]** — `cj-code` 29 entities, `cj-rules` 27, `bridge_edges` 4,417 — so a run
today would not be vacuous. That is luck, not a guard. Cheapest item in the cluster; still live.

## 6. nmemo-jcj — doc-number collisions — **STILL-BROKEN, materially worse than recorded**

Enumerated all four trees under `docs/architecture/` **[V]**. **43 numbers resolve to more than one file.**
Worst cases:

| number | candidates | files |
|---|---|---|
| 23 | **8** | `cross-corpus-audit/23-concept-convergence-prereg.md`, `single-graph/23-prereg-embedding-upgrade.md`, `truth-graph/23-topology-primitives.md` + `23.1`–`23.5` |
| 24 | 5 | cca/24, sg/24, tg/24 + `24.1`, `24.2` |
| 38 | **4** | `cross-corpus-audit/38-single-graph-hardening.md`, `single-graph/38-dedup-hardening-design.md`, `truth-graph/38-graph-anchored-fallback-retrieval.md`, `truth-graph/38-parallel-ingestion.md` |
| 39 | **4** | `cross-corpus-audit/39-known-truths.md`, `single-graph/39-temporal-fact-model.md`, `truth-graph/39-graph-validity-harness.md`, `truth-graph/39-natural-language-graph-querying.md` |
| 35 | **4** | `cross-corpus-audit/35-adversary-brief.md`, **`cross-corpus-audit/35-multihop-concept-recall-prereg.md`**, `single-graph/35-provenance-lineage-backbone-design.md`, `truth-graph/35-reconciliation-agent.md` |
| 0,1,2 | 4 each | all three arch trees + `token-usage/` |
| 3-22, 25-34, 36-42 | 3 each | cca + sg + tg all run these numbers |

Corrections to the bead **[V]**:

1. "doc 39 has **three** candidates" is now **four**; "doc 38" also four. The bead predates `single-graph/` 38-47.
2. The bead missed an **intra-tree** collision: `cross-corpus-audit/` itself has **two files numbered 35**
   (`35-adversary-brief.md`, `35-multihop-concept-recall-prereg.md`). So the "two doc 38s / two doc 39s" pattern
   is not unique to `truth-graph/`.
3. **`token-usage/` is a fourth colliding tree** (00-02), unmentioned.
4. **The bead's own mitigation has failed.** It says "New work under `docs/architecture/single-graph/` starts a
   clean series to avoid adding to it." `single-graph/` now runs **00-47**, which overlaps `cross-corpus-audit/`
   (00-42) and `truth-graph/` (00-42) at *every single number*. Starting a fresh series in a flat numeric
   namespace is what took "doc 39" from 3 candidates to 4. The new docs 44-47 the caller flagged are only the
   tail of this; 44/45/46/47 are currently *unique* (no other tree reaches 43+), but 00-42 are all triple-booked.

**[I]** Option (a) from the bead (tree-prefixed citations: `tg-39`, `cca-39`, `sg-39`) is the only one of the
three that fixes the 43 existing collisions without a rename storm. Option (c) (full relative paths) is already
what `CLAUDE.md` mandates and is not being followed by the bead corpus itself.

## 7. nmemo-7ra — demonstrate fallback-recall uplift — **PARTIAL (re-scope, do not close as stale)**

**Pushing back: this one is not stale.** The mechanism is wired into a live HTTP read path **[V]**:

- `platform/src/index.ts:1446-1515` (found with `grep -a` — the NUL-byte trap): `computeQueryFallbackEvidence`
  imports `{ flatRetrievalFailed, recallViaGraph }` at `:1458`, calls `recallViaGraph(queryVector, flatHits, …)`
  at `:1474`, tags results `source: 'graph_fallback'` at `:1490`, and `:1504-1515` hands the result to
  `invokeReasoningAgent` for `POST /api/reason/query`.
- `causal-agent.ts:27,1915` uses the same gate.
- `recallViaGraph` (`graph-fallback.ts:460-479`) and `expandFromAnchors` (`:172-199`) both exist and were
  *modernised*, not deleted, by the AGE retirement (`:199` now calls `traverseFromEntities`).
- The eval harness and fixture still exist: `platform/src/test/integration/fallback-recall-eval.test.ts`,
  `platform/src/test/fixtures/fallback-hobbies-session.json`.

**Do docs 22/47 falsify it?** They lower the prior hard but test a **different regime [V + I]**:

- Doc 22 (`22-results-traversal-augmented.md`) and doc 47 (`47-traversal-substrate-prereg.md` RESULTS) both
  test traversal as an **always-on third RRF signal** on the **papers-as-queries target-finding** task.
  Doc 47: `T-R4 − FACTNAME` = −0.0169 (dal) / −0.0336 (arxiv), both spanning 0; `T4 − L3` = −0.0491 **below 0**
  on arxiv. TRAV alone 0.0904/0.0724 vs NAME 0.2006/0.1912.
- 7ra tests traversal as a **failure-gated recovery** (`flatRetrievalFailed` → top score < floor) on a
  **multi-session personal-memory needle**. Different task, different corpus, different trigger. Not tested.
- The strongest transferable evidence against it is doc 47's artifact control: `T-R4 − T-R4-RAND` spans 0 and
  on arxiv the **degree-preserving shuffled graph scores nominally better** (0.2661 vs 0.2300) — so on that
  task the traversal signal is a **degree prior, not graph structure**. **[I]** If that generalises, 7ra's
  `expandFromAnchors` walk has no structural signal to recover with. But doc 47 itself caveats
  "one traversal formulation tested … a different seeding or weighting is untested", and its blind adversary
  was **NOT RUN**.

**Two hard substrate blockers, verified [V]:**

```sql
select count(*) total, count(source_memory_id) with_src from facts;  -- 343018 | 0
select count(*) from fact_units;                                     -- 0
```

`graph-fallback.ts:270-300` resolves each neighbour fact through `fact_units` → Qdrant to get `unitText`. With
zero `fact_units` rows and `source_memory_id` NULL everywhere, the fallback on the live substrate returns
neighbour facts with **no evidence text**, so 7ra's acceptance criterion ("recovers ≥1 answer window") is
unachievable there. Root cause located: `fact_units` is written **only** by the legacy path
(`pipeline.ts:685`); the epoch/promote path deliberately does not (`promotion.ts:517-525`: *"source_text is the
proposer's reasoning here (the epoch path emits no verbatim span), so unit-grained fact_units offset spans stay
a Phase-4 extraction change"*). The 294-doc substrate came through the epoch path. **[I]** The experiment is
therefore still runnable, but only via a `pipeline.ts`-ingested fixture — which is what the existing eval test
does — not against the production graph.

**Recommendation [I]:** do not close as stale. Either (a) park it with the reason recorded as
"traversal measured null in three adjacent pre-registrations (docs 22, 47); failure-gated regime untested but
prior is strongly negative; blocked on the Phase-4 `fact_units` extraction change", or (b) re-scope it to
depend on the provenance backbone (`single-graph/35-provenance-lineage-backbone-design.md`). Closing it as
"decided-against" would be defensible; closing it as "stale premise" would be **wrong** — the code is live.

## 8. nmemo-eue — LongMemEval baseline — **DECIDED-AGAINST (parked). NOT completed by docs 42/43.**

**The caller's belief is wrong, and this is the most important correction in the batch.** Docs 42/43 do not
satisfy this bead's acceptance criteria, and in one case directly contradict them.

Bead asks for **[C, `bd show`]**: full 500-question LongMemEval_S; each session replayed turn-by-turn through
`POST /ingest`; each question via `POST /query`; a **Sonnet judge call per question**; five-category accuracy
plus overall plus envelope; `docs/benchmarks/results/longmemeval.md` regenerated; **abstention strictly between
0% and 100%**; upstream submodule pinned.

What docs 42/43 actually did **[V, from the docs]**:

| criterion | docs 42/43 |
|---|---|
| 500-question cut | **No** — doc 42 n=**150** (single-session types), doc 43 n=**121**. 271 of 500. |
| turn-by-turn `POST /ingest` | **No** — Path B is "session-as-document"; doc 43:82 *"Deterministic + Claude-free: nomic via Ollama … **no LLM judge, no extraction, no DB**"*. |
| `POST /query` | **No** — offline embedding harness. |
| Sonnet judge per question | **No** — doc 43:87 *"Get consent before any extraction / **QA-judge** / Path-A spend."* |
| five-category accuracy | **No** — metrics are session `recall@k` / `nDCG@10`, not QA accuracy. |
| abstention in (0,1) | **Directly contradicted** — doc 42 §2: abstention members *"excluded — retrieval eval skips abstention"*. |
| Mnemo's own pipeline exercised | **Explicitly deferred** — both docs: *"does NOT exercise the R4 entity⊕fact lever — that is Path A (deferred)"*. |

So docs 42/43 are a **different experiment** (retrieval floor on real queries, to close the papers-as-queries
validity gap) that happens to share a dataset. They are strong work and both carry blind adversaries — they
just are not this bead.

**What the bead's own pipeline has actually achieved [V]:**

- The QA pipeline is **built**: `benchmarks/longmemeval/run.py` has `run_real`, `JUDGE_PROMPT_VERSION`
  (`:409`), and the trend writer. Submodule **is** pinned: `git submodule status` →
  `9e0b455f4ef0e2ab8f2e582289761153549043fc benchmarks/longmemeval/upstream`. So two acceptance sub-criteria
  (infra + pin) are met.
- `docs/benchmarks/results/longmemeval.md` has six rows. **Five are n=1.** The only multi-question row
  (2026-06-02, n=21, `overall_accuracy=0.524`, `abstention_rate=0.667`, `sanity_pass=True`) is a **dry run** —
  `run.py:145` `score = 1.0 if idx % 2 == 0 else 0.0` (11/21 = 0.5238, 2/3 = 0.6667) and `run.py:180`
  `judge_prompt_version="dry-run"`, a field the trend table does not render. Confirms
  `00-consolidated-keep-list.md:160`. **The only encouraging number in the results doc is synthetic.**
- **`sanity_pass` is structurally unreachable for small N, verified independently:** `score.py:76` requires
  the abstention rate strictly in (0,1); `run.py:257` samples `questions[: config.sample_size]` (a **prefix**);
  and loading the dataset myself gives 500 questions with **30 abstention items, first at index 64**
  (contiguous 64,65,66,67,68,…). So any run with `sample_size ≤ 64` has zero abstention items → `sanity_pass`
  False → `run.py:428` exits 1. Every real run was n=1.

**Decision of record [C]:** `docs/architecture/single-graph/00-consolidated-keep-list.md:160` —
*"**Parked by user decision.**"* `CLAUDE.md` "Decided, do not relitigate" — *"LongMemEval is parked."*

**Verdict: DECIDED-AGAINST (parked by user), not done.** Close it as parked with that reason — do **not** close
it as "satisfied by docs 42/43", which would bank a QA-accuracy claim that was never measured. Two residuals
worth their own P3 beads **[I]**: (1) the prefix sampler makes the pre-registered sanity gate unreachable below
N=65 — either shuffle/stratify or lower the gate; (2) a dry-run row sits in the published trend table
indistinguishable from a real one, because the dashboard drops `judge_prompt_version`.
