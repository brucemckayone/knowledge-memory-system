# Web framework
fastapi==0.109.0
uvicorn[standard]==0.27.0
pydantic==2.5.3

# ML / AI
ollama==0.1.6  # For embeddings (local)
openai>=1.0.0  # For text generation via Z.AI API
faster-whisper>=0.10.0

# HTTP client (for downloading audio and web scraping)
httpx==0.25.2

# File uploads
python-multipart==0.0.6

# Web scraping (W13)
readability-lxml>=0.8.1
beautifulsoup4>=4.12.0
lxml>=5.0.0

# Document parsing (W37)
PyMuPDF>=1.24.0
python-docx>=1.1.0
PyYAML>=6.0

# Phase 2 — graph topology (truth-graph doc 23). manylinux wheels on x86_64;
# no system C-lib needed. networkx is pure-python; python-igraph ships
# pre-built binaries; leidenalg pinned for the upcoming 23.4 community
# detection. psycopg gives the sidecar direct Postgres access for the
# graph export step (master §2.3).
python-igraph>=0.11
networkx>=3.2
leidenalg>=0.10
psycopg[binary]>=3.1,<4

# Phase 3 — semantic clustering (truth-graph doc 24.1). HDBSCAN cosine
# clustering over entity_meta.centroid. hdbscan brings scikit-learn as a
# transitive dep; we pin sklearn explicitly per the scoping note. Pre-built
# wheels on manylinux x86_64 and win_amd64.
hdbscan>=0.8
scikit-learn>=1.3
