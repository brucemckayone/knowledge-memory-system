# Phase 4 Blast Radius Analysis — baseline (2026-04-28)

## Setup

- **Data set version:** v1.0
- **Fixtures:** `platform/src/test/data/phase4-blastradius/fixtures/{small-graph-5-nodes, linear-chain-depth-10, diamond-topology, cycle-topology, orphan-node, fan-out-explosion, deep-cycle-depth-20, realistic-500-entity}.sql`
- **Expected:** `platform/src/test/data/phase4-blastradius/expected/fan-out-explosion.expected.json`
- **Code commit:** `da0730e` (state at the start of C5; final benchmark numbers gathered after this commit lands)
- **Machine:** Windows 11 Enterprise, Docker Desktop, PostgreSQL 17 + pgvector + Apache AGE on port 5433

## Scope

This baseline covers the 8 fixtures that ship with Phase 4:

| Fixture | Level | Purpose |
|---------|-------|---------|
| small-graph-5-nodes | L1 | Deterministic 5-entity / 5-fact / 4-event topology — fixture-loader smoke |
| linear-chain-depth-10 | L1 | Walks a 10-node chain — verifies maxDepth cap & full-walk correctness |
| diamond-topology | L1 | Multi-path convergence — verifies DISTINCT ON shortest-reach |
| cycle-topology | L1 | 3-node cycle — verifies path-accumulator cycle protection (small) |
| orphan-node | L1 | Isolated entity — verifies empty-report contract |
| fan-out-explosion | adversarial | 1 fact cited by 500 active edges — citation perf + zero-mutation under hypothetical=expire |
| deep-cycle-depth-20 | adversarial | 20-node cycle — verifies cycle protection at scale |
| realistic-500-entity | L2 | 100 entities / 500 facts / 999 active edges — perf target |

## Results

- **Assertions:** 59 / 59 inline `expect()` plus 4 fixture-driven side-effect assertions
- **Duration:** 12.24s total test execution (vitest run on the full Phase 4 suite)
- **DB state after fan-out fixture:** 1 entity, 1 fact, 501 events, 500 edges, 500 edge_source_refs

## Metrics

| Metric | Value | Target | Pass |
|--------|-------|--------|------|
| `analyzeImpact` (realistic-500-entity, depth=3) | <1000ms (test cap) | <500ms (spec) | △ wall-clock larger than spec target on workstation Docker (see Findings); test cap honoured for CI determinism |
| `analyzeImpact` (fan-out-explosion, fact root, hypothetical=expire) | <1500ms (test cap) | <500ms (spec) | △ same workstation overhead caveat |
| Cycle termination (deep-cycle-depth-20, maxDepth=10) | <2000ms | terminates without infinite loop | ✓ |
| No double-counting (deep cycle, diamond) | 100% (Set size === array length) | 100% | ✓ |
| maxDepth respected (linear-chain depth=3 vs depth=10) | 3 → 3 edges; 10 → 9 edges | exact | ✓ |
| Hypothetical zero-mutation (fan-out 500 edges) | All `expired_at IS NULL`; 0 fact_history rows | 100% | ✓ |
| Severity scoring correctness | 100% across 8 inline rule tests | 100% | ✓ |
| Citation correctness (Phase 3 index) | 500 / 500 fan-out edges discovered | 100% | ✓ |
| Pattern impact (Phase 6 forward-compat) | empty when no patterns; correctly joins when seeded | 100% | ✓ |
| MCP tool callable via handleToolCall | JSON.stringify report round-trips | round-trip OK | ✓ |
| HTTP endpoint | 200/400/404 paths verified | exact | ✓ |

## Findings

**All correctness assertions pass.** The 9-rule severity table, hypothetical zero-mutation invariant, cycle protection, citation correctness, and pattern-impact forward compat all behave per spec.

**Performance is correct but slower than the spec target on this workstation.** The recursive CTE (forward+backward 2-hop) terminates correctly on every fixture, but realistic-500-entity at depth=3 measured ~6s in the test wall clock vs the spec's <500ms target. The internal `analyzeImpact` measurement (between two `Date.now()` calls inside the test) was ~1s — the rest is fixture load + PG round-trips + vitest overhead. The spec's <500ms target was set against an unspecified machine; on this Docker-on-Windows setup the round-trips dominate.

The bidirectional CTE was tightened during C5: an earlier draft included "alternate cause / alternate effect" sibling-edge hops that produced a quadratic fan-out on hub events (250k row-pairs at depth 2 on the fan-out fixture, blowing past the 30s vitest timeout). The current 2-hop pattern (forward: `next.cause = chain.effect`; backward: `next.effect = chain.cause`) matches the existing `traceCauses` / `projectTrajectory` shape and is what the spec section "Transitive Chains" actually shows.

The ZERO-mutation contract under `hypothetical='expire'` is verified twice: once inline (facts.expired_at, invalid_at, causal_edges.expired_at, fact_history) and once via the assertion-runner against the 500-edge fan-out fixture.

## Next Iteration

- **Re-measure on Linux CI** — the Docker-on-Windows round-trip cost is a known factor. The spec's <500ms target should be re-checked against a Linux runner; if it still misses there, investigate query plan (hash join vs nested loop on the `edge_source_refs` lookup).
- **Level-3 fixture (`misra-chapter-impact.sql`)** — graduation criterion for `klv.4`. Real MISRA-derived dependencies validate severity scoring against a domain story rather than synthetic topology.
- **Investigate retroactive cycle protection on `traceCauses` / `projectTrajectory`** — filed as `nmemo-8vq.1`. Phase 4's CTE owns its own protection; the existing functions don't.
- **Adversarial: severity rule gaming** — build a graph where every dependent scores critical, assert the summary tally accurately reflects that without capping. Out of scope for this baseline; defer to klv.4.
- ~~**Hypothetical-mode consistency** — currently only `expire` re-scores citations to critical. `invalidate` and `weaken` flow through the API but don't yet alter the rule table. Spec doesn't require it; flag for klv.4 design discussion.~~ Resolved by R11/F6 (nmemo-2yv.104): `invalidate` and `weaken` removed from the API.

## Raw Log

```
$ pnpm vitest run src/test/harness/blast-radius.test.ts

Test Files  1 passed (1)
     Tests  59 passed (59)
  Duration  12.24s

Per-test highlights:
  Foundation:                                       3 tests, 49-94ms
  Direct dependents:                                9 tests, 20-60ms
  Transitive chains:                                8 tests, 17-85ms
  Citation dependents:                              4 tests, 22-59ms
  Severity scoring:                                 8 tests, 24-50ms
  Hypothetical mode:                                4 tests, 36-48ms
  Pattern impact:                                   4 tests, 21-31ms
  MCP tool:                                         3 tests, 10-26ms
  HTTP endpoint:                                    7 tests, 6-75ms
  Fixture-driven smoke:                             6 tests, 22-81ms
  Adversarial deep-cycle:                           1 test, 119ms
  Adversarial fan-out:                              1 test, 2844ms (fixture load + 500 edges + assertion runner)
  Performance realistic-500:                        1 test, 6084ms (1000-edge fixture load dominates)
```

---

## Re-measurement (2026-05-26) — bead nmemo-2yv.108

### Verdict: **Target met on Windows + Docker** (Linux re-measure not strictly needed)

The bead's locked Decision (2026-05-22) called for a Linux re-measure first, with query-plan investigation only if the miss persists. When the cycle reached this bead, the Windows + Docker measurement was re-run to confirm the baseline number. The result is dramatically different from the 2026-04-28 baseline — the spec target is now comfortably met on the *same* hardware where it previously appeared to miss.

### Setup

- **Code commit:** `06405d5` (latest on `bead/92-cross-cluster-runs-telemetry`, head of the implementation cycle)
- **Machine:** Windows 11 Enterprise, Docker Desktop, PostgreSQL 17 + pgvector + Apache AGE on port 5433 (unchanged from baseline)
- **Date:** 2026-05-26
- **Fixture:** `realistic-500-entity.sql` (unchanged from baseline — same 100 entities, 500 facts, 999 active edges, depth-3 root at event `20000007-0000-0000-0000-000000000001`)

### Measurements (5 sequential runs)

The internal `[impact]` log line emits `duration_ms` from inside `analyzeImpact` between two `Date.now()` calls, capturing the service-level delta (excluding fixture load + vitest overhead):

| Run | Internal `duration_ms` | Test wall-clock |
|-----|------------------------|-----------------|
| 1   | 35 ms                  | 150 ms          |
| 2   | 30 ms                  | (filtered)      |
| 3   | 29 ms                  | (filtered)      |
| 4   | 29 ms                  | (filtered)      |
| 5   | 26 ms                  | (filtered)      |
| 6   | 27 ms                  | (filtered)      |

- **Median internal:** ~29 ms
- **Max internal across 6 runs:** 35 ms (cold-run)
- **Spec target:** <500 ms
- **Test cap:** <1000 ms (kept; see "Test cap review" below)

The first run is cold (no driver / no connection-pool warm-up); steady-state is ~27-30 ms. **The spec target is met with ~14x headroom** even on the platform that originally appeared to miss it.

### What changed since baseline

The baseline (2026-04-28) reported `~1s internal time`. The current internal delta is ~30 ms — a 33x improvement on the same workstation Docker setup. No commits specifically targeted `analyzeImpact` performance between baseline and now, but the surrounding area saw substantial changes:

- `nmemo-2yv.102` (blast-radius preflight audit columns) — added columns the recursive CTE now joins; the join pattern may have changed shape.
- `nmemo-2yv.105` (observability log line) — refactored `scoreSeverity` neighbourhood (~150 lines), incidentally optimised hot path.
- `nmemo-2yv.106` (Rule 1b tier split) — modified severity scoring path.
- `nmemo-2yv.87` (advisory locks on compute endpoints) — improved Postgres connection reuse, may have reduced round-trip overhead seen by neighbouring tests.

Pinning the exact cause is out of scope for this bead — the question the bead asks (is the spec target met?) is answered affirmatively without that investigation.

### Linux re-measure — deferred

Per the bead's locked Decision, Linux re-measurement was the gating step *before* any query-plan work. Two facts make the Linux measurement no longer load-bearing:

1. **Windows + Docker already meets the target with 14x headroom.** Linux is generally faster than Windows + Docker for PG round-trips — if Windows passes by 14x, Linux is implied-safe by a wider margin.
2. **The implementer's environment has no readily-available Linux runtime.** Only `docker-desktop`'s internal WSL distro is registered (`wsl.exe -l -v` → `docker-desktop Running 2`); no real Linux distro is installed and the project has no CI configured (`.github/workflows/` absent).

If a Linux runner becomes available later (CI rollout, WSL2 distro install, dev VM), the protocol below can be executed in <5 min to capture a Linux number for completeness.

### Linux re-measurement protocol (for future execution)

When a Linux environment is available, follow these steps to capture the Linux-internal delta:

1. **Ensure Postgres + AGE are running on the Linux host** (Docker on Linux is fine; the test reads `DATABASE_URL` and connects to whatever's there).
   ```bash
   make up   # from repo root — brings up postgres + qdrant
   ```

2. **Run the perf test in isolation:**
   ```bash
   pnpm vitest run src/test/harness/blast-radius.test.ts -t "realistic-500-entity"
   ```

3. **Capture the internal delta.** Look for the `[impact]` log line; the `duration_ms` field is the internal `analyzeImpact` time:
   ```
   [impact] actor=unknown root_type=causal_event root_id=...001 depth=3 ... duration_ms=NN
   ```

4. **Run 5-6 times** to capture warm-up + steady-state. Median across the steady-state runs is the comparable number.

5. **Compare to the Windows + Docker median of ~29 ms.** Linux is expected to be at-or-below that. If Linux shows >100 ms internal delta, that would be unexpected and worth investigating (likely environmental, not query-plan, unless the Postgres versions differ).

6. **If — against expectation — the Linux internal delta exceeds 500 ms**, file a follow-up bead for query-plan investigation per the bead's Decision branch-B. Capture `EXPLAIN ANALYZE` of the recursive CTE in `src/services/impact.ts` against the realistic-500-entity fixture.

### Test cap review

The test cap at `blast-radius.test.ts:1639` is `expect(elapsed).toBeLessThan(1000)`. With the Windows + Docker median at ~30 ms and a worst-case cold-run of 150 ms wall-clock (35 ms internal), there is substantial headroom. Options considered:

- **Keep at 1000 ms.** Existing 2x-spec buffer; absorbs CI variance, VM thrashing, and slower future hardware without flake.
- **Tighten to 500 ms** (= spec target). Tighter signal but risks flake on a slow CI host or under contention; the wall-clock includes fixture load + setup, which is more variable than the internal delta.
- **Tighten to 200 ms.** Would catch a regression promptly but is too close to current cold-run (150 ms wall-clock).

**Decision: keep the 1000 ms cap.** The 14x internal-delta headroom over spec is real but the wall-clock cap also covers fixture-load + Postgres warmup, which are platform-sensitive and not the perf target. A future bead may add a separate inline assertion on the *internal* `duration_ms` (parsing the `[impact]` log line) — that would catch regressions on the actual perf metric without coupling to fixture-load variance. Out of scope here.

### Summary

- ✅ Spec target (<500 ms internal) **met** on Windows + Docker with ~14x headroom (median 29 ms).
- ⏸ Linux re-measurement **deferred** (no Linux environment available; Windows-pass implies Linux-pass given typical platform ordering).
- 📋 Linux protocol **documented** above for future execution if/when CI lands.
- ✅ Test cap (1000 ms) **kept** — current spec target met, 2x headroom absorbs CI variance, internal-delta assertion deferred to future bead.
- ❌ Query-plan investigation **not triggered** (the "if persists" branch of the locked Decision does not fire because the miss does not persist).
